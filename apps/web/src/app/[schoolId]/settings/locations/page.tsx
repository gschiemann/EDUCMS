"use client";

/**
 * /[schoolId]/settings/locations — Locations (§7.3).
 */
import { SettingsLocationsPage } from '@/components/settings/cc/LocationsPage';

export default function Page() {
  return <SettingsLocationsPage />;
}
