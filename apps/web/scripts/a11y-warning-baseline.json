{
  "warningBaseline": 1,
  "lockedAt": "2026-05-31",
  "note": "Real count locked 2026-05-31 from the CI a11y run (audit §18 P0): 0 errors, 1 warning across all 10 audited routes. The 999 bootstrap ceiling is retired — the gate now actually blocks regressions (any new moderate/minor warning fails CI). DOWN-only: never raise without a justification comment here AND fixing the new warning(s). COVERAGE CAVEAT: the CI harness runs UNAUTHENTICATED (no DB seed / login), so authed tenant routes render their login/shell state — this count covers public renders. Auth-ing the harness + adding the builder route to ROUTES is the follow-up to widen coverage (audit §18 P1)."
}
