# Default Android proguard-rules placeholder. Manager has very few
# reflective surfaces (ContentProvider, BroadcastReceivers, Service —
# all referenced from manifest, which AGP keeps automatically) so no
# custom rules are needed. Release build still uses Android's default
# proguard-android-optimize.txt rules.
