const fs = require('fs');

let c = fs.readFileSync('apps/web/src/components/widgets/WidgetRenderer.tsx', 'utf8');

const imports = [
"// AUTOGENERATED IMPORTS",
"import { BackToSchoolText, BackToSchoolClock, BackToSchoolAnnouncement, BackToSchoolCalendar, BackToSchoolStaff, BackToSchoolCountdown, BackToSchoolLogo, BackToSchoolTicker, BackToSchoolWeather, BackToSchoolImageCarousel } from './themes/back-to-school';",
"import { BusLoopText, BusLoopClock, BusLoopTicker, BusLoopWeather, BusLoopAnnouncement, BusLoopCalendar } from './themes/bus-loop';",
"import { DinerChalkboardText, DinerChalkboardClock, DinerChalkboardLunchMenu, DinerChalkboardAnnouncement, DinerChalkboardCountdown, DinerChalkboardTicker, DinerChalkboardCalendar, DinerChalkboardStaff, DinerChalkboardLogo, DinerChalkboardWeather, DinerChalkboardImageCarousel } from './themes/diner-chalkboard';",
"import { FinalChanceClock, FinalChanceWeather, FinalChanceText, FinalChanceLogo, FinalChanceAnnouncement, FinalChanceCalendar, FinalChanceCountdown, FinalChanceStaff, FinalChanceImageCarousel, FinalChanceTicker } from './themes/final-chance';",
"import { AthleticsLogo, AthleticsCountdown, AthleticsAnnouncement, AthleticsText, AthleticsTicker } from './themes/high-school-athletics';",
"import { LibraryQuietText, LibraryQuietClock, LibraryQuietImage, LibraryQuietRichText, LibraryQuietLunch, LibraryQuietTicker } from './themes/library-quiet';",
"import { MSHallClock, MSHallBellSchedule, MSHallTicker, MSHallAnnouncement, MSHallImageCarousel, MSHallWeather, MSHallText, MSHallCountdown, MSHallStaff, MSHallLogo } from './themes/middle-school-hall';",
"import { MusicArtsText, MusicArtsCountdown, MusicArtsRichText, MusicArtsSpotlight, MusicArtsTicker } from './themes/music-arts';",
"import { StemScienceText, StemScienceCountdown, StemScienceRichText, StemScienceImageCarousel, StemScienceTicker } from './themes/stem-science';",
"import { SunshineAcademyClock, SunshineAcademyWeather, SunshineAcademyCountdown, SunshineAcademyText, SunshineAcademyAnnouncement, SunshineAcademyTicker, SunshineAcademyCalendar, SunshineAcademyStaffSpotlight, SunshineAcademyImageCarousel } from './themes/sunshine-academy';"
].join('\n');

c = c.replace(/} from 'lucide-react';/, "} from 'lucide-react';\n" + imports);

// Widget mapping:
const injections = {
  'ClockWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolClock config={config} compact={compact} />;",
    "  if (config.theme === 'bus-loop') return <BusLoopClock config={config} compact={compact} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardClock config={config} compact={compact} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceClock config={config} compact={compact} />;",
    "  if (config.theme === 'library-quiet') return <LibraryQuietClock config={config} compact={compact} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallClock config={config} compact={compact} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyClock config={config} compact={compact} />;"
  ],
  'WeatherWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolWeather config={config} compact={compact} />;",
    "  if (config.theme === 'bus-loop') return <BusLoopWeather config={config} compact={compact} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardWeather config={config} compact={compact} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceWeather config={config} compact={compact} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallWeather config={config} compact={compact} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyWeather config={config} compact={compact} />;"
  ],
  'CountdownWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolCountdown config={config} compact={compact} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardCountdown config={config} compact={compact} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceCountdown config={config} compact={compact} />;",
    "  if (config.theme === 'high-school-athletics') return <AthleticsCountdown config={config} compact={compact} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallCountdown config={config} compact={compact} />;",
    "  if (config.theme === 'music-arts') return <MusicArtsCountdown config={config} compact={compact} />;",
    "  if (config.theme === 'stem-science') return <StemScienceCountdown config={config} compact={compact} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyCountdown config={config} compact={compact} />;"
  ],
  'TextWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolText config={config} />;",
    "  if (config.theme === 'bus-loop') return <BusLoopText config={config} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardText config={config} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceText config={config} />;",
    "  if (config.theme === 'high-school-athletics') return <AthleticsText config={config} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallText config={config} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyText config={config} />;",
    "  if (config.theme === 'library-quiet') return config.html ? <LibraryQuietRichText config={config} /> : <LibraryQuietText config={config} />;",
    "  if (config.theme === 'music-arts') return config.html ? <MusicArtsRichText config={config} /> : <MusicArtsText config={config} />;",
    "  if (config.theme === 'stem-science') return config.html ? <StemScienceRichText config={config} /> : <StemScienceText config={config} />;"
  ],
  'AnnouncementWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolAnnouncement config={config} compact={compact} />;",
    "  if (config.theme === 'bus-loop') return <BusLoopAnnouncement config={config} compact={compact} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardAnnouncement config={config} compact={compact} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceAnnouncement config={config} compact={compact} />;",
    "  if (config.theme === 'high-school-athletics') return <AthleticsAnnouncement config={config} compact={compact} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallAnnouncement config={config} compact={compact} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyAnnouncement config={config} compact={compact} />;"
  ],
  'TickerWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolTicker config={config} />;",
    "  if (config.theme === 'bus-loop') return <BusLoopTicker config={config} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardTicker config={config} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceTicker config={config} />;",
    "  if (config.theme === 'high-school-athletics') return <AthleticsTicker config={config} />;",
    "  if (config.theme === 'library-quiet') return <LibraryQuietTicker config={config} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallTicker config={config} />;",
    "  if (config.theme === 'music-arts') return <MusicArtsTicker config={config} />;",
    "  if (config.theme === 'stem-science') return <StemScienceTicker config={config} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyTicker config={config} />;"
  ],
  'BellScheduleWidget': [
    "  if (config.theme === 'middle-school-hall') return <MSHallBellSchedule config={config} compact={compact} />;"
  ],
  'LunchMenuWidget': [
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardLunchMenu config={config} compact={compact} />;",
    "  if (config.theme === 'library-quiet') return <LibraryQuietLunch config={config} compact={compact} />;"
  ],
  'CalendarWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolCalendar config={config} compact={compact} />;",
    "  if (config.theme === 'bus-loop') return <BusLoopCalendar config={config} compact={compact} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardCalendar config={config} compact={compact} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceCalendar config={config} compact={compact} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyCalendar config={config} compact={compact} />;"
  ],
  'StaffSpotlightWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolStaff config={config} compact={compact} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardStaff config={config} compact={compact} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceStaff config={config} compact={compact} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallStaff config={config} compact={compact} />;",
    "  if (config.theme === 'music-arts') return <MusicArtsSpotlight config={config} compact={compact} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyStaffSpotlight config={config} compact={compact} />;"
  ],
  'ImageWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolLogo config={config} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardLogo config={config} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceLogo config={config} />;",
    "  if (config.theme === 'high-school-athletics') return <AthleticsLogo config={config} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallLogo config={config} />;",
    "  if (config.theme === 'library-quiet') return <LibraryQuietImage config={config} />;"
  ],
  'ImageCarouselWidget': [
    "  if (config.theme === 'back-to-school') return <BackToSchoolImageCarousel config={config} />;",
    "  if (config.theme === 'diner-chalkboard') return <DinerChalkboardImageCarousel config={config} />;",
    "  if (config.theme === 'final-chance') return <FinalChanceImageCarousel config={config} />;",
    "  if (config.theme === 'middle-school-hall') return <MSHallImageCarousel config={config} />;",
    "  if (config.theme === 'stem-science') return <StemScienceImageCarousel config={config} />;",
    "  if (config.theme === 'sunshine-academy') return <SunshineAcademyImageCarousel config={config} />;"
  ]
};

for (const funcName of Object.keys(injections)) {
  const lines = injections[funcName];
  const searchStr = "function " + funcName + "({ config, compact }: { config: any; compact: boolean }) {";
  const searchStr2 = "function " + funcName + "({ config }: { config: any }) {";
  
  if (c.includes(searchStr)) {
    c = c.replace(searchStr, searchStr + '\n' + lines.join('\n'));
  } else if (c.includes(searchStr2)) {
    const linesNoCompact = lines.map(l => l.replace(/ compact=\{compact\}/g, ''));
    c = c.replace(searchStr2, searchStr2 + '\n' + linesNoCompact.join('\n'));
  } else {
    console.log('Failed to find', funcName);
  }
}

fs.writeFileSync('apps/web/src/components/widgets/WidgetRenderer.tsx', c);
console.log('Done');
