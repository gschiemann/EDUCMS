# §3–§5 AI Audit — Providers · Feature Surfaces · Competitive Scan

**Audit date:** 2026-05-30 · read-only, every documented feature traced to real callers · bar = real gap today.

**Headline:** this cluster is in markedly better shape than the 2026-05-28 synthesis implies — three of its AI flags (alt-text quota fork, multi-replica cap, temperature parity) are now FIXED in current code. **No correctness/security P0 or P1 in this cluster.** The only material exposure is competitive (missing AI image-gen + translation) plus latent cost-waste/staleness.

## 1. Coverage table

| § | D | UX | F | Status |
|---|---|----|----|--------|
| 3 AI providers × entry points (Anthropic/OpenAI/Google) | A | A | **A−** | COVERED. Quota disambiguation, encryption, AuditLog, AbortSignal, temperature parity, multi-replica caps all real. 2 latent P2s. |
| 4 AI feature surfaces | A | A− | **B+** | COVERED. Sparkle (4 live mounts), touch-template, alt-text wired end-to-end. image-gen/translation/TTS/voice-SOS/auto-celebration-ML/anomaly-ML genuinely N-A. |
| 5 AI comparative scan | — | — | **C+** | COVERED. Ahead on copy-gen + alt-text; behind on AI image-gen + translation (HIGH gaps). |

## 2. Findings

**Resolved since synthesis (verified):** P1-7 alt-text quota fork FIXED (`ai-alt-text.service.ts:512-526` routes all providers through `mapProviderQuotaError`; Google vision added `:599`). P1-14 multi-replica cap FIXED (`ai.service.ts:109-179` moved both hourly caps to Redis sorted sets; monthly cap in Postgres). Temperature parity FIXED (`ai-providers.ts:325` pins 0.7 on all three; reasoning models omit it; spec `ai-providers.spec.ts:44-82`).

| Sev | § | File:line | Costume? | Finding & fix |
|-----|---|-----------|----------|---------------|
| **P2** | 3 | `ai-providers.ts:158,173,203,243` | No | **Model-catalog staleness.** Defaults `claude-3-5-haiku`, `claude-opus-4-20250514`, `gpt-5`, `gemini-2.5-pro` aging by 2026-05. Mitigated (fallback `dispatchAi:315` + documented quarterly refresh) but Premium-tier operators pay for current models — refresh the `id:` strings. |
| **P2** | 3 | `ai-providers.ts:337-360` vs `bug-analyzer.service.ts:617-620` | No | **Anthropic prompt caching on only 1 of 6 fetch sites.** `cache_control:{type:'ephemeral'}` used in bug analyzer but NOT in user-facing text-gen/touch-template/alt-text Anthropic calls (static system prompts re-sent every call). Cost waste on platform-paid generations — add `cache_control` to the system block. |
| **P2** | 3/14 | `ai.service.ts:85-98` | No | **No per-vertical system prompts.** `SYSTEM_PROMPTS` keyed by intent (announcement/quote/menu_item…), not vertical. Vertical only interpolated into the user prompt (`:466`). The §14 "per-vertical AI prompts" surface is genuinely thin. |
| **P3** | 4 | `ai-alt-text.service.ts:377` | No | Alt-text hourly 30/hr Redis cap NOT shared with sparkle — an operator could exceed 30/hr by mixing sparkle + bulk uploads. Monthly cap + per-call cost still bound spend. |

**Verified usable end-to-end (not assumed):** Sparkle (`AiGenerateButton` at 4 live mounts in PropertiesPanel: 2338/2424/2642/4876 — the rendered V2 builder per rule #9 → `POST /ai/generate`, Zod+RBAC+audit; FE disambiguates `AI_PROVIDER_OUT_OF_CREDIT` vs `AI_CAP_REACHED`); touch-template (`POST /templates/generate-touch`, hard server-side sanitizer: widget allowlist, percent clamps, SSRF via `validatePublicUrl`, proto-pollution scrub); alt-text (upload-completion fire-and-forget `assets.controller.ts:176` + regen `:1045`, audited); BYOK lifecycle (`ai-key-cipher.ts` AES-256-GCM envelope w/ `DEVICE_SECRET_KEY`; test-on-save burns 10-token call + logs `AI_KEY_TEST_FAILED`; DELETE=revocation; `keyHealthy` flag).

## 3. §5 Competitive-gap list

| Feature | Incumbents shipping it | VenueOS | Severity |
|---|---|---|---|
| AI text/copy gen in widgets | OptiSigns/ScreenCloud partial | **✓ ahead (live displayed copy, intent-aware)** | — |
| AI alt-text / auto-caption | none | **✓ ahead (3 providers + vision)** | — |
| AI full-template synthesis | OptiSigns | **✓ parity/ahead (touch-template)** | — |
| **AI image generation** | OptiSigns ships it; Canva-class | **✗ NOT BUILT** | **HIGH — biggest visible gap** |
| **AI translation / multilingual** | OptiSigns, ScreenCloud | **✗ NOT BUILT** | **HIGH — K-12 ELL + V2 multilingual emergency depends on it** |
| TTS for emergency announcements | BrightSign (HW) | ✗ | MED — V2 multi-modal; life-safety adjacency |
| CV asset auto-tagging | OptiSigns | ✗ | LOW-MED (alt-text vision infra is 80% there) |
| Smart playlist/scheduling suggestions | partial | ✗ | LOW |
| AI bg-removal | OptiSigns | ✗ | LOW |

Net: **VenueOS leads the field on the two AI features that touch on-screen content (copy-gen + alt-text)** — a defensible differentiator. Trails on AI image-gen + translation (both buyer-visible).

## 4. Biggest risk
No technical P0/P1 — the AI provider layer is production-grade. Material exposure is **competitive (missing AI image-gen + translation)** plus a latent **cost-waste/staleness pair** (Anthropic prompt-cache in 1/6 sites; aging Premium-tier model IDs operators pay for).
