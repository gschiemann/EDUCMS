# FINAL PRE-LAUNCH BETA — Master Tracker (resumable)

**Started 2026-06-26.** Greg: *"fire up the beta testing team and keep testing the entire app, from a one-user setup to a multi-franchise setup managing hundreds of screens with all the integrations in use… this is the final beta team that is getting us ready to launch. UX, UI, Functionality, Technical integrations, Emergency triggering, Template creation, AI features at the highest level. I want an extensive report when we are done beta testing PER AGENT, then we decide what to go fix."*

> **HOW TO RESUME (read this first if you're picking up after a reset):**
> 1. Read this file top-to-bottom. The **WAVE STATUS** table is the source of truth — find the first wave not `DONE`.
> 2. Per-agent reports land in `agents/<wave><n>-<scope>.md`. Findings get rolled into `01-CONSOLIDATED-FINDINGS.md` (severity-ranked) as waves complete.
> 3. Continue from the first non-DONE wave. Do NOT re-run DONE waves. Update the WAVE STATUS table + RUNNING LOG after every agent returns (persist-on-return — never trust chat memory).
> 4. The fix phase does NOT start until Greg says so. This campaign is FIND + REPORT only.

## Guardrails (do not violate)
- **Live prod is the target** (venue-os.app / api-production-39a1.up.railway.app). No real customers yet → aggressive testing OK.
- **Every test agent self-provisions its OWN isolated throwaway tenant** via `POST /api/v1/signup`. NEVER touch Greg's live **water-polo "Dodgers" tenant** or his physical LED. NEVER finalize his live game.
- **Emergency + destructive flows** (trigger/all-clear, delete, wipe) ONLY on isolated throwaway tenants.
- **No password typing** by computer-use; sign in only on pre-filled forms. Don't drive Greg's desktop while he's on it.
- **Verify before claiming** — a finding must have repro + evidence (screenshot path / curl / DB query / log line). "Looks broken" without repro is not a finding.
- This is FIND-only. No code fixes mid-campaign unless Greg says.

## Scope matrix — every cell must be covered before "done"
**Scale tiers:** S1 one-user single-location · S2 single-venue power use (live sports / QSR-POS) · S3 multi-franchise HQ (hundreds of screens, all integrations).
**Dimensions (Greg's list):** UX · UI · Functionality · Technical integrations · Emergency triggering · Template creation · AI features.
**Domain map:** the CLAUDE.md **Standard Audit Surface §1–§21** (every section reported covered / N-A / deferred). Each wave maps to surface sections.

## WAVE STATUS  (the resume table)
| Wave | Focus | Scale | Surface §§ | Status | Agent reports |
|------|-------|-------|-----------|--------|---------------|
| **A** | One-user setup + core operator journey (signup→onboard→brand→pair→asset→playlist→schedule→live) | S1 | 2,19,20 | **DONE** (run wf_c39ab1c4-bbc #2; 5/5 reports; 0 P0 / 5 P1 / 17 P2) | agents/A1..A5 ✅ |
| **B** | Template creation + editor editability + AI graceful-degradation + kiosk scenes | S1/S2 | 3,4,5,19 | **DONE** (wf_49eda8a1-798; 5/5 reports; 0 P0 / 7 P1 / 13 P2; verdict: editor+boards+player-scene-runtime are REAL not costumes; launch items = AI-multiscene sceneId bug + dead StyleDisclosure + Athletics dead tab) | agents/B1..B5 ✅ |
| **C** | Emergency triggering end-to-end (code+curl reality; live-glass deferred) | S1/S2 | 1,16 | **DONE** (28 REAL / 0 costume / 0 P0 / 0 P1 / 4 P2; "signed-WS theater" finding genuinely CLOSED; grade A/A-/A-) | agents/CD1 ✅ |
| **D** | Technical integrations reality — REAL/COSTUME/NOT-BUILT from code+curl (live happy-path deferred to sandbox keys) | S2 | 3,6,7,8,9,10,12,13 | **DONE** (48 REAL / 1 costume / 25 not-built; **1 P0** = POS webhook CSRF block, lead-verified; 3 P1; near-zero costumes) | agents/CD2..CD5 ✅ |
| **AI-full** | Full AI surface completeness — every entry point × provider (§3), every AI work-surface (§4), competitive comparative (§5) | S1/S2 | 3,4,5 | **DONE** (14 REAL / 0 costume / 0 P0 / 0 P1 / 5 P2; grade A/A-/A) | agents/CD6 ✅ |
| **E** | Multi-franchise / HQ at scale (fleet roll-up, cross-location publish, fleet alerting, 100s of screens) | S3 | 11,14,17 | **DONE** (SHIP-READY; 0 P0 / 1 P1 / 5 P2; all 3 fleet phases work live; isolation holds; only P1 = PILOT_SEAT_LIMIT=1000 config; grade A-) | agents/E1 ✅ |
| **F** | Cross-cutting: cross-browser (WebKit/Chromium-83), mobile-iPhone UX, a11y, perf, security/multi-tenant isolation | all | 15,17,18,21 | **DONE** (cross-browser GO / mobile GO / a11y PASS / **security: multi-tenant isolation AIRTIGHT under live exploit**, 1 NEW P1 = dead rate-limit; 0 P0 / 2 P1 / 13 P2) | agents/F1..F4 ✅ |
| **LIVE-AI** | Lead computer-use on Greg's authed Dodgers tenant w/ REAL OpenAI key — image-gen, vision alt-text, template-gen, chat-to-edit, sparkle | S1/S2 | 3,4,5,19 | **DONE** (grade A-/A/A-; 1 P1 found+FIXED live `06038352`, 5 P2; all 5 AI surfaces verified working live) | agents/LIVE-ai.md ✅ |
| **LIVE-glass** | Lead computer-use: physical LED via webcam/ScreenConnect — render fidelity, LED-fit, emergency override on real glass, celebrations | S2 | 1,15 | PENDING (needs cam on + login) | this file, LIVE section |

## Per-agent report convention
Each agent writes a FULL report to `agents/<wave><n>-<scope>.md` BEFORE returning a summary, containing: surface tested, scale tier, step-by-step what-it-did, findings table (severity P0/P1/P2 · area · what · repro · evidence path), coverage (what was NOT reached + why), and an overall letter grade per Greg's 3 lenses (Design / UX / Functionality). Screenshots → session scratchpad, path cited in the report.

## NEEDED FROM GREG (unblocks the gated waves)
- ✅ Cam on (ScreenConnect "Video Preview") + I'm logged into the app + session kept alive — for the LIVE track.
- ⚠️ **AI provider key** in prod (ANTHROPIC/OPENAI/GOOGLE) — without it ALL AI features (sparkle, image-gen, brand-voice, chat-edit, alt-text) only show graceful-degradation; can't test the happy path. Biggest unblock for Wave B.
- ⚠️ **Integration sandbox keys** for Wave D happy-paths: Stripe test keys (billing), Square/Toast/Clover sandbox (POS menu boards), an SSO test IdP, any streaming source. Without them, agents verify only the "not configured" UX, not the live integration.
- Confirm: aggressive prod testing OK + the isolated-tenant guardrail above is the rule.

## RUNNING LOG (append-only; newest last)
- 2026-06-26 — Campaign created. Wave A launched (run wf_c39ab1c4-bbc; 5 agents A1–A5).
- 2026-06-26 — Wave A run #1 ALL 5 agents died on the session usage-limit before writing any report (`reports:[]`, 0 files). Limit reset → Wave A RE-LAUNCHED (task `wrfjqf8kt`, same run id resumed). Agents write report-to-disk BEFORE returning, so a mid-wave limit now preserves finished agents' reports. CADENCE: run one wave at a time (5 agents ≈ burns a big chunk of the shared limit in ~150s); on completion roll findings → 01-CONSOLIDATED-FINDINGS.md, update WAVE STATUS, launch next wave. Awaiting cam/login + AI key + integration sandbox keys for B/D/LIVE.
- 2026-06-26 — **Wave B DONE** (wf_49eda8a1-798, 5/5 reports): 0 P0 / 7 P1 / 13 P2. Verdict: editor + 107 EXTERNAL_HTML boards + player scene-runtime are REAL (no costumes). Launch items: B5 AI-multi-scene `sceneId` drop → empty destination scenes (root cause of the live-observed empty scenes) + no player empty-scene fallback; B1 dead StyleDisclosure (rich text editor, 0 call sites) + quick-bar missing line-height/align/brand presets; B4 "Athletics" dead category tab + no grid empty-state. B3 watch-item: AI Tier-1/2 budget safety hinges on ANTHROPIC_API_KEY being UNSET on prod. Rolled → 01-CONSOLIDATED.
- 2026-06-26 — **Wave E+F DONE → AUDIT PHASE COMPLETE** (wf_8c4d7745-9a0, 5/5 reports). All 21 Standard-Audit-Surface sections now covered across A + LIVE-AI + B + C+D + E+F. E1 scale SHIP-READY (fleet works live, isolation holds). F1 cross-browser/Taurus GO. F2 mobile/iPhone GO (address picker now fully fixed — Google key live). F3 a11y PASS. F4 security: **multi-tenant isolation AIRTIGHT under live two-tenant exploitation** (the #1 launch risk — confirmed). **NEW P1 (lead-verified by curl): per-IP rate-limiting is DEAD in prod** — 8 bad logins → 0 × 429, x-ratelimit-remaining bounced 9,9,9,8,8,8,9,8 (in-memory throttler split across Railway replicas; needs Redis-backed store). **Final tally: 1 P0 (POS webhook CSRF) · 17 P1 · 72 P2 (+1 P1 image-gen already fixed).** Two security items need Greg's fix decision: POS-webhook-CSRF (P0) + dead-rate-limit (P1). Config-only (Greg): PILOT_SEAT_LIMIT=1000, AI Tier-1 env-var, rotate secrets, Stripe live keys. Only LIVE-glass (live-emergency-on-real-LED) remains, gated on webcam.
- 2026-06-26 — **Wave C+D+AI-full DONE** (wf_48bc4d5b-7b5, 6/6 reports). **Integration reality verdict: 76 REAL · ~1 COSTUME · 39 NOT-BUILT.** The app is overwhelmingly real, not a costume — emergency moat is the most-hardened surface (signed-WS theater genuinely closed), sports-data/score path is real+deployed, POS has 4 live DIRECT connectors, AI surface fully wired, public-alert honestly not-built (no life-safety costume). **ONE P0: POS webhooks (`/pos/webhook/custom-webhook` + `/webhook/square`) 403 CsrfError in prod — never added to csrf EXEMPT_PATHS; lead curl-VERIFIED (403 vs billing-webhook 400). One-line fix, safe (webhooks self-auth via HMAC).** 3 P1 (CD2 dead-end copy amplifies the P0; CD4 TOTP task-hygiene + push token gap). Rolled → 01-CONSOLIDATED. **Remaining: Wave E (scale) + Wave F (cross-cutting security/a11y/perf/mobile) + LIVE-glass (cam). Decision pending from Greg: fix the POS-webhook P0 now (clean one-liner) or batch to fix-phase.**
- 2026-06-26 — **Wave C+D+AI-full LAUNCHED** (bg workflow wf_48bc4d5b-7b5, 6 agents) in response to Greg's "did we audit every technical integration?" — honest answer was NO, so closing it: CD1 emergency end-to-end (code+curl; live-glass still deferred), CD2 POS/commerce reality, CD3 sports-data+streaming reality, CD4 comms+auth/SSO/SIS reality, CD5 design-imports+public-alert reality, CD6 full AI surface completeness (§3×§4×§5 beyond the live happy-path). Each classifies every provider REAL/COSTUME/NOT-BUILT/DEFERRED w/ file:line. On completion roll → 01-CONSOLIDATED, then E (scale) + F (cross-cutting security/a11y/perf/mobile) remain, + LIVE-glass (cam).
- 2026-06-26 — **Wave B LAUNCHED** (bg workflow `wf_49eda8a1-798`, 5 read-only audit agents B1-B5): B1 widget editability depth (§19), B2 EXTERNAL_HTML 107-board shim/click-edit sweep, B3 AI graceful-degradation (no-key path — complements LIVE-AI happy-path), B4 gallery/layout/cross-browser, B5 kiosk multi-scene player runtime. Scoped to NOT re-test the live AI happy-path. Agents write reports to agents/B*.md + return summaries. On completion: roll → 01-CONSOLIDATED-FINDINGS.md, update WAVE STATUS, launch next wave (C emergency / E scale / F cross-cutting; D + LIVE-glass gated).
- 2026-06-26 — **LIVE-AI track DONE.** Greg entered a real OpenAI key (BYOK) in Settings→AI on Dodgers. Lead drove the live authed browser (Chrome MCP) through all 5 AI surfaces: image-gen (square ✅; landscape/portrait found 400-broken → FIXED `06038352` per-model size re-map + 3 specs → Railway redeployed → re-verified live ✅), vision alt-text (A/A/A — IDs Cleveland Browns from pixels), template-gen (3 drafts, real multi-scene GOTO-SCENE wiring, A-/B+/A), chat-to-edit diff-preview (A/A), sparkle 3-options w/ tone (A/A/A). 1 P1 (fixed) + 5 P2 (entity `&amp;`, brand-palette-in-template-gen, K-12 copy on non-K12, chat-edit size direction, asset 320px resolution→A2). Test template created + DELETED (tenant clean). Report: agents/LIVE-ai.md. **Next: Wave B (templates+editor, can now use the live key) OR LIVE-glass (needs cam) OR Waves C/E/F. Awaiting Greg's go + integration sandbox keys for D.**
