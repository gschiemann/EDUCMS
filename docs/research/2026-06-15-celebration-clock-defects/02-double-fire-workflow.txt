{
  "summary": "Find every code path that can make a sports celebration fire twice, with file:line evidence",
  "agentCount": 9,
  "logs": [
    "parallel[4] failed: agent({schema}): subagent completed without calling StructuredOutput (after 2 in-conversation nudges)"
  ],
  "result": {
    "allFindings": [
      {
        "vector": "Board cue-pump dedup gaps: can SAME logical cue be pushed to cueQueue twice or played twice via gaps in seenCues (by cue.id) and lastCueSig dedup (key/team/6s window)?",
        "verdict": "POSSIBLE",
        "rootCause": "Board cue-pump dedup is sound (seenCues by id + lastCueSig by key/team/6s), but water-polo CTS scenario uses CustomScoreboardScene with embedded CtsCelebrationOrchestratorWidget that independently fires the same cue feed. Two separate animation engines (board's CueOverlay + widget's cinematics) both render on same activeCue state, creating double-fire to crowd.",
        "evidence": [
          "apps/web/src/app/board/[gameId]/page.tsx:3941-3942 seenCues.has(c.id) dedup",
          "apps/web/src/app/board/[gameId]/page.tsx:3953-3957 lastCueSig dedup condition",
          "apps/web/src/app/board/[gameId]/page.tsx:4169-4186 custom template path renders CueOverlay",
          "apps/web/src/app/board/[gameId]/page.tsx:4242-4264 legacy scene path renders CueOverlay",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1519-1541 CtsCelebrationOrchestratorWidget listens to same cue feed independently",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1462-1475 fire() method triggers setActive without checking board's activeCue state",
          "apps/api/src/sports/sports.service.ts:3150-3162 maybeAutoCelebrate creates AUTO cue with team set",
          "apps/api/src/sports/sports.service.ts lines 3094-3136 separate MANUAL cues can fire after AUTO within 6s if dedup fails"
        ],
        "proposedFix": "Unify cue firing: either (1) gate CtsCelebrationOrchestratorWidget to only fire if board's CueOverlay is NOT active, or (2) remove duplicate listeners and route all cues through single fire path. Check custom scoreboard templates for redundant celebration widgets that listen to same cue feed.",
        "confidence": 0.65
      },
      {
        "vector": "Two CueOverlay mount sites: custom template path renders CueOverlay at zIndex 50; custom template can also embed CtsCelebrationOrchestratorWidget which independently polls same cue feed and fires cinematics",
        "verdict": "CONFIRMED",
        "rootCause": "When Game.scoreboardTemplateId is set, board page renders CustomScoreboardScene (line 4157) + CueOverlay (line 4170). If the custom template contains a CtsCelebrationOrchestratorWidget zone, it independently polls /sports/board/:id (line 1419) and fires cinematics on goal-delta (lines 1478-1500). Same cue from data.cues triggers BOTH: (1) board's CueOverlay.celebrationSrc() iframe at zIndex 50, and (2) widget's CUE_CATALOG[cueId].Component at zIndex z.zIndex (from zone, defaults to 1 but can be high). Both render full-screen overlays with position:absolute; top/right/bottom/left: 0.",
        "evidence": [
          "apps/web/src/app/board/[gameId]/page.tsx:4139-4188 — custom template path with early return, mounts both CustomScoreboardScene and CueOverlay",
          "apps/web/src/app/board/[gameId]/page.tsx:4169-4186 — CueOverlay renders at zIndex 50",
          "apps/web/src/app/board/[gameId]/CustomScoreboardScene.tsx:194-215 — zones render widgets via WidgetPreview, each zone has zIndex from template",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1380-1642 — CtsCelebrationOrchestratorWidget renders full-screen absolute overlay (lines 1624-1629), can fire cinematics",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1418-1419 — widget polls useRibbonBoardData(cueGameId) independently",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1478-1500 — goal-delta detection fires cinematics from CUE_CATALOG",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1637-1638 — renders CUE_CATALOG[cueId].Component which is the same cinematic source as CueOverlay",
          "apps/web/src/app/board/[gameId]/page.tsx:3476-3516 — CueOverlay.celebrationSrc() returns iframe for cinematic celebration"
        ],
        "proposedFix": "When a custom scoreboard template is loaded (scoreboardTemplateId set), disable automatic celebration firing in any embedded widget that reads from /sports/board cues. Two approaches: (1) Filter cues in CtsCelebrationOrchestratorWidget to exclude target='BOARD' or 'ALL' when embedded in a custom template zone; add a prop to signal context. (2) Have board page NOT mount CueOverlay when template contains a celebration widget zone — let template zone handle all celebrations. (3) Simplest: add a global flag (e.g., window.BOARD_HANDLES_CELEBRATIONS = true) when board mounts CueOverlay, so any widget checking this skips its own celebration firing for BOARD/ALL-targeted cues.",
        "confidence": 0.95
      },
      {
        "vector": "Server emits TWO cues per goal: an AUTO-celebrate cue AND a separate CTS-ingest cue record",
        "verdict": "RULED_OUT",
        "rootCause": "n/a",
        "evidence": [
          "apps/api/src/sports/sports.service.ts:4678-4706 — CTS ingest path emits ONE SCORE event (line 4688) followed by ONE maybeAutoCelebrate call (line 4696), which fires ONE CUE event (line 3150) with `auto:true` per scoring team. No second cue is created.",
          "apps/api/src/sports/sports.service.ts:3150 — maybeAutoCelebrate creates ONE record(id, 'CUE', {...}) call per firable team, not two. The firable array is filtered by CELEBRATION_MUTEX_MS to suppress operator-cue-within-window scenarios.",
          "apps/api/src/sports/sports.service.ts:3566-3584 — fireCue (manual path) records ONE CUE event per manual fire, identified by unique GameEvent.id.",
          "The server GameEvent.id is unique per write (Prisma auto-generates). Once created, a CUE event is never duplicated. The /sports/board endpoint returns json.cues as gameEvent rows within a 20s window (line 464: CUE_FEED_WINDOW_MS)."
        ],
        "proposedFix": "n/a",
        "confidence": 0.95
      },
      {
        "vector": "A CTS celebration WIDGET on a custom scoreboard fires in ADDITION to the board route's cue overlay, causing celebrations to fire twice on a single scoring event",
        "verdict": "CONFIRMED",
        "rootCause": "Two or more celebration-firing components co-exist on the same custom scoreboard template (CtsCelebrationWidget + CtsCelebrationOrchestratorWidget + board route's CueOverlay) and all subscribe to the same CTS game-state feed via useCtsGameState(). When a score increases, each component's independent useEffect detects the delta and independently fires its own celebration, with no deduplication logic between them. The code comment at PropertiesPanel.tsx line 3452-3454 explicitly documents this: \"both fire together if both are mounted on the same canvas.\"",
        "evidence": [
          "CtsRibbonWidgets.tsx:1087 — CtsCelebrationWidget calls useCtsGameState() and subscribes to edu:cts-game-state",
          "CtsRibbonWidgets.tsx:1096-1107 — CtsCelebrationWidget's useEffect fires on snap.homeScore/awayScore delta with its own refs (lastHomeRef, lastAwayRef)",
          "CtsRibbonWidgets.tsx:1392 — CtsCelebrationOrchestratorWidget also calls useCtsGameState() independently",
          "CtsRibbonWidgets.tsx:1478-1500 — CtsCelebrationOrchestratorWidget's useEffect fires on same score deltas with separate refs (lastHomeRef, lastAwayRef)",
          "PropertiesPanel.tsx:3452-3454 — Comment states 'both fire together if both are mounted on the same canvas'",
          "page.tsx:4169-4186 — CustomScoreboardScene renders CueOverlay independently on BOARD/ALL cues, third fire path",
          "CustomScoreboardScene.tsx:207-213 — WidgetPreview renders each zone's widget, allowing both celebration widgets + board CueOverlay to co-exist independently"
        ],
        "proposedFix": "Implement shared deduplication state across all celebration-firing components on custom scoreboards. Extract a custom hook (e.g. useCelebrationDedup) that maintains single source-of-truth for fired celebrations, shared by both widgets and board route. Alternatively: disable CtsCelebrationWidget by default when CtsCelebrationOrchestratorWidget is present, or consolidate both widgets into one composable that switches between simple-pulse and cinematic modes based on config.",
        "confidence": 0.99
      },
      {
        "vector": "Dual delivery (WebSocket + HTTP poll): Does the board route receive cues from MORE THAN ONE source (e.g. a WS/SSE subscription AND the HTTP poll of /sports/board/:id)? If a cue arrives via two channels, do both enqueue/play it?",
        "verdict": "RULED_OUT",
        "rootCause": "n/a",
        "evidence": [
          "apps/web/src/app/board/[gameId]/page.tsx:3922-3970 — The board route has exactly ONE cue source: a 750ms HTTP poll of GET /sports/board/:id. No WebSocket, SSE, or EventSource subscriptions exist in the file (confirmed via grep for WebSocket/socket/SSE/subscribe — only noise matches on unrelated words like \"subscription\" in comments and \"shows\" in text).",
          "apps/web/src/app/board/[gameId]/page.tsx:3945-3962 — All cues enqueue from the polled json.cues array and are deduplicated by the lastCueSig logic before being pushed to cueQueue.",
          "apps/web/src/app/board/[gameId]/page.tsx:4139-4194 (custom template path) and 4196+ (legacy path) — The two CueOverlay render locations are in mutually exclusive return branches: if scoreboardTemplateId is set, line 4155 returns early and the legacy CueOverlay at line 4242 never executes.",
          "apps/web/src/components/widgets/sports/GameStateContext.tsx:110-130 — Even the nested GameStateProvider inside CustomScoreboardScene polls /sports/board/:id independently, but this is the SAME endpoint and uses the same deduplication logic (applyCtsOverlay), so it cannot introduce a second cue stream to the parent page."
        ],
        "proposedFix": "n/a",
        "confidence": 0.99
      }
    ],
    "confirmedAfterVerify": [
      {
        "vector": "Two CueOverlay mount sites: custom template path renders CueOverlay at zIndex 50; custom template can also embed CtsCelebrationOrchestratorWidget which independently polls same cue feed and fires cinematics",
        "verdict": "CONFIRMED",
        "rootCause": "When Game.scoreboardTemplateId is set, board page renders CustomScoreboardScene (line 4157) + CueOverlay (line 4170). If the custom template contains a CtsCelebrationOrchestratorWidget zone, it independently polls /sports/board/:id (line 1419) and fires cinematics on goal-delta (lines 1478-1500). Same cue from data.cues triggers BOTH: (1) board's CueOverlay.celebrationSrc() iframe at zIndex 50, and (2) widget's CUE_CATALOG[cueId].Component at zIndex z.zIndex (from zone, defaults to 1 but can be high). Both render full-screen overlays with position:absolute; top/right/bottom/left: 0.",
        "evidence": [
          "apps/web/src/app/board/[gameId]/page.tsx:4139-4188 — custom template path with early return, mounts both CustomScoreboardScene and CueOverlay",
          "apps/web/src/app/board/[gameId]/page.tsx:4169-4186 — CueOverlay renders at zIndex 50",
          "apps/web/src/app/board/[gameId]/CustomScoreboardScene.tsx:194-215 — zones render widgets via WidgetPreview, each zone has zIndex from template",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1380-1642 — CtsCelebrationOrchestratorWidget renders full-screen absolute overlay (lines 1624-1629), can fire cinematics",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1418-1419 — widget polls useRibbonBoardData(cueGameId) independently",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1478-1500 — goal-delta detection fires cinematics from CUE_CATALOG",
          "apps/web/src/components/widgets/sports/CtsRibbonWidgets.tsx:1637-1638 — renders CUE_CATALOG[cueId].Component which is the same cinematic source as CueOverlay",
          "apps/web/src/app/board/[gameId]/page.tsx:3476-3516 — CueOverlay.celebrationSrc() returns iframe for cinematic celebration"
        ],
        "proposedFix": "When a custom scoreboard template is loaded (scoreboardTemplateId set), disable automatic celebration firing in any embedded widget that reads from /sports/board cues. Two approaches: (1) Filter cues in CtsCelebrationOrchestratorWidget to exclude target='BOARD' or 'ALL' when embedded in a custom template zone; add a prop to signal context. (2) Have board page NOT mount CueOverlay when template contains a celebration widget zone — let template zone handle all celebrations. (3) Simplest: add a global flag (e.g., window.BOARD_HANDLES_CELEBRATIONS = true) when board mounts CueOverlay, so any widget checking this skips its own celebration firing for BOARD/ALL-targeted cues.",
        "confidence": 0.95,
        "verifier": {
          "stillStands": true,
          "reasoning": "The prior analysis claiming a double-fire vector is CONFIRMED by code inspection. The mechanism is technically sound and reachable:\n\n1. RENDER PATHS CONFIRMED: When Game.scoreboardTemplateId is set, board page renders both CustomScoreboardScene (mounting widgets from template) and CueOverlay (mounting celebration iframe) at position:absolute overlaying each other (page.tsx:4139-4188, lines 4157 + 4169).\n\n2. WIDGET FIRING CONFIRMED: CtsCelebrationOrchestratorWidget independently polls /sports/board/:id (line 1419), detects goal-delta changes (lines 1478-1500), and fires cinematics from CUE_CATALOG (lines 1624-1629). Widget is registered as SPORTS-vertical available for custom templates (variants-register.ts:1188).\n\n3. CUE ROUTING CONFIRMED: Both systems process the same cue from data.cues. Board's cuePlaysHere() filters RIBBON-only (page.tsx:186-188). Widget's target filter accepts ALL/BOARD/null cues (CtsRibbonWidgets.tsx:1530-1531). Same source, same cue id, both pass target filter.\n\n4. INSUFFICIENT DEDUP CONFIRMED: Board uses 6s dedup window with KEY+team-wildcard (page.tsx:3953-3958). Widget uses independent 2s CUE_COOLDOWN with separate lastFireCueRef tracking (CtsRibbonWidgets.tsx:1430-1432, 1465-1472). Each system fires independently if within its own cooldown window.\n\n5. FULL-SCREEN OVERLAYS CONFIRMED: CueOverlay renders zIndex 50 iframe (page.tsx:3495-3514). Widget renders CUE_CATALOG component at position:absolute with opacity fade (CtsRibbonWidgets.tsx:1624-1629). Both visible simultaneously when firing in lockstep.\n\nPath is reachable in water-polo+CTS setup IF the custom scoreboard template contains CtsCelebrationOrchestratorWidget zone. No guard prevents this configuration."
        }
      },
      {
        "vector": "A CTS celebration WIDGET on a custom scoreboard fires in ADDITION to the board route's cue overlay, causing celebrations to fire twice on a single scoring event",
        "verdict": "CONFIRMED",
        "rootCause": "Two or more celebration-firing components co-exist on the same custom scoreboard template (CtsCelebrationWidget + CtsCelebrationOrchestratorWidget + board route's CueOverlay) and all subscribe to the same CTS game-state feed via useCtsGameState(). When a score increases, each component's independent useEffect detects the delta and independently fires its own celebration, with no deduplication logic between them. The code comment at PropertiesPanel.tsx line 3452-3454 explicitly documents this: \"both fire together if both are mounted on the same canvas.\"",
        "evidence": [
          "CtsRibbonWidgets.tsx:1087 — CtsCelebrationWidget calls useCtsGameState() and subscribes to edu:cts-game-state",
          "CtsRibbonWidgets.tsx:1096-1107 — CtsCelebrationWidget's useEffect fires on snap.homeScore/awayScore delta with its own refs (lastHomeRef, lastAwayRef)",
          "CtsRibbonWidgets.tsx:1392 — CtsCelebrationOrchestratorWidget also calls useCtsGameState() independently",
          "CtsRibbonWidgets.tsx:1478-1500 — CtsCelebrationOrchestratorWidget's useEffect fires on same score deltas with separate refs (lastHomeRef, lastAwayRef)",
          "PropertiesPanel.tsx:3452-3454 — Comment states 'both fire together if both are mounted on the same canvas'",
          "page.tsx:4169-4186 — CustomScoreboardScene renders CueOverlay independently on BOARD/ALL cues, third fire path",
          "CustomScoreboardScene.tsx:207-213 — WidgetPreview renders each zone's widget, allowing both celebration widgets + board CueOverlay to co-exist independently"
        ],
        "proposedFix": "Implement shared deduplication state across all celebration-firing components on custom scoreboards. Extract a custom hook (e.g. useCelebrationDedup) that maintains single source-of-truth for fired celebrations, shared by both widgets and board route. Alternatively: disable CtsCelebrationWidget by default when CtsCelebrationOrchestratorWidget is present, or consolidate both widgets into one composable that switches between simple-pulse and cinematic modes based on config.",
        "confidence": 0.99,
        "verifier": {
          "stillStands": true,
          "reasoning": "The prior analysis identifying a double-fire vector is CONFIRMED as real and reachable in the VenueOS water polo + CTS scoreboard setup.\n\nCONFIRMED EVIDENCE:\n\n1. **Two independent celebration components on custom scoreboard templates:**\n   - CtsCelebrationWidget (id: 'scoreboard-cts-celebration') registered as composable widget at variants-register.ts:1164-1169\n   - CtsCelebrationOrchestratorWidget (id: 'scoreboard-cts-celebration-orchestrator') registered at variants-register.ts:1183-1188\n   - Both CAN be placed on the same custom template via the builder (CustomScoreboardScene.tsx:194-215 renders each zone's widget independently)\n\n2. **Both subscribe to independent but overlapping score-delta triggers:**\n   - CtsCelebrationWidget:1087 calls useCtsGameState(liveSurface === true)\n   - CtsCelebrationWidget:1096-1107 useEffect fires on snap.homeScore/awayScore delta\n   - CtsCelebrationOrchestratorWidget:1392 also calls useCtsGameState(liveSurface === true)\n   - CtsCelebrationOrchestratorWidget:1478-1500 useEffect fires on same score deltas with separate refs\n\n3. **A third independent fire path via board CueOverlay:**\n   - board/[gameId]/page.tsx:4139-4188: When data.scoreboardTemplateId is set (custom template), CueOverlay is mounted at line 4169-4186\n   - board/[gameId]/page.tsx:3920-3974: Board page polls /sports/board/:id endpoint and queues auto-generated cues from the feed\n   - CueOverlay renders when activeCue is set, independently of the widget fires\n\n4. **No cross-component deduplication exists:**\n   - The comment at PropertiesPanel.tsx:3452-3454 explicitly states: 'both fire together if both are mounted on the same canvas'\n   - CtsCelebrationWidget has its own lastHomeRef/lastAwayRef at line 1089-1091\n   - CtsCelebrationOrchestratorWidget has separate lastHomeRef/lastAwayRef at line 1403-1406\n   - No shared state between them on the same template\n   - CueOverlay is completely independent, fired from page's activeCue state set by poll loop\n\n5. **The server does create AUTO cues on score delta:**\n   - sports.service.ts:3078-3162 maybeAutoCelebrate() creates ONE CUE record per scoring event\n   - This cue gets polled by the board and queued into CueOverlay\n\n6. **Reachability in water polo CTS setup:**\n   - Water polo is sport: 'WATERPOLO' (per task #40, #124, #130)\n   - CTS bridge pumps edu:cts-game-state CustomEvent on score changes\n   - CustomScoreboardScene passes live=true to WidgetPreview (line 212)\n   - Both CTS widgets activate when live=true and score delta is detected\n   - The /sports/board/:id feed also carries the auto-generated cue\n   - All three paths fire independently with zero coordination\n\nThe mechanism is real, the code path is reachable, and the setup (water polo + custom scoreboard template with both CTS widgets) is operationally deployed."
        }
      }
    ]
  }
}