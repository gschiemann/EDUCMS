# Editor-vs-Canva Deep Dive — captured lenses (5/7; ai-creator-flow + architecture-seams pending re-run)

Recovered 2026-07-02 from workflow wf_e3dc5383-71d after the session-limit kill.


## Lens: canva-benchmark

VERDICT: VenueOS does NOT need full Canva parity to crush the signage field — it needs (1) a small MUST-MATCH set whose absence reads amateur, and (2) to make its moat LOUD inside the editor. (a) MUST-MATCH (all cited): in-editor stock photo/element search (Canva 100M+ assets; OptiSigns Designer 2.0 ships it), multi-select align/distribute + grouping (canva.com/help/layer-group-align; OptiSigns component groups), one-click background removal (Canva Magic Studio; OptiSigns 2.0 auto-detects+removes), canvas resize/magic-switch (Canva Magic Switch; OptiSigns 2.0 canvas resizing), reusable components/brand-locked templates (OptiSigns component library; ScreenCloud Canvas locked elements). Already at parity — do NOT rebuild: smart-guide snapping (snap-engine.ts), multi-select+marquee (BuilderCanvas.tsx:294-440), full keyboard set incl. Cmd+/ shortcuts modal (BuilderShell.tsx:428-529), undo/redo, layers panel, per-field StyleableField typography, click-to-edit V6, 3-candidate AI. (b) THE OPENING: every signage competitor's editor is weak and they OUTSOURCE design to Canva — Yodeck reviews: "editor functional but not intuitive... image editing is limited," and a Canva mistake must be "corrected in Canva then pushed anew" (static round-trip); OptiSigns editor called "cumbersome and less intuitive compared to other popular design tools" (GetApp); ScreenCloud Canvas has only ~140 templates; Rise Vision leans on Canva-modify + Google Sheets. Nobody owns a native editor good enough to kill the round-trip — that seat is empty. (c) MOAT (none can copy): data-bound zones (REST/Sheets PropertiesPanel.tsx:1352, POS auto-86 menus, sports consoles/CTS, emergency), fleet publish (150-screen cross-location), per-screen canvas sizes incl. LED ribbons/Taurus, App Library paste-first. All exist; all are invisible at design time. (d) THE 3 MOAT FEATURES TO PUT FRONT-AND-CENTER: 1) "Put on screens" publish button in the builder header (ScreenCloud's whole pitch, ours is buried outside the editor), 2) LIVE badges + real tenant data rendered on canvas (the board that updates itself — Canva designs on screens are frozen pixels), 3) "Adapt to my screens" auto-resize using the per-screen canvases we already know (Canva Magic Switch can't know your fleet). Rise Vision now ships a 3-design AI generator, so our 3-candidate flow is table stakes — differentiate by making the tenant-data grounding visible.

### [P0] No in-editor stock photo/graphics search — operator must leave the app to find an image (Canva's core habit loop) _(effort M)_
- **Today:** AssetPickerField in the builder is own-uploads/URL only (apps/web/src/components/template-builder/PropertiesPanel.tsx:2728, 3174-3175, 3331); the free Pexels pipeline already exists but is server-side-only for AI boards (apps/api/src/ai/stock-image.service.ts) — grep shows zero Pexels/stock UI under apps/web/src/components/template-builder/
- **Canva:** Canva: 100M+ photos/elements searchable in the left rail (canva.com). OptiSigns Designer 2.0 ships in-editor imagery + auto background removal (optisigns.com/post/optisigns-designer-2-0-whats-new-improved). Rise Vision embeds Canva-modify (risevision.com blog).
- **Fix:** Add a 'Find a photo' tab inside the existing AssetPickerField backed by the already-configured Pexels proxy (PEXELS_API_KEY, $0), pre-seeded with a query auto-derived from the tenant vertical + zone context; one tap inserts and re-hosts to Supabase (same attachKeptBoardPhoto path). No new settings.

### [P0] Builder dead-ends at Save — no publish-to-screens from inside the editor (our fleet is the moat and the editor hides it) _(effort M, MOAT)_
- **Today:** BuilderShell panel keys are widgets/apps/background/layers/scenes/properties/brand/review (BuilderShell.tsx:52); grep for publish across template-builder/*.tsx finds only a comment in TemplatePreviewModal.tsx:28 — Cmd+S saves (BuilderShell.tsx:433-437) and the operator must exit to playlists/schedules to get the design on glass. Cross-location fleet publish exists elsewhere (task #233) and per-screen canvas sizes are known (#123).
- **Canva:** ScreenCloud Canvas headline: 'make changes to your designs and they'll automatically update across your screens... edit designs and publish from anywhere' (screencloud.com/apps/canvas). Canva reaches TVs via marketplace apps (EasyTV, Display NOW) (canva.com/apps).
- **Fix:** 'Put on screens' primary button in the builder header → screen/group picker showing each screen's real canvas thumbnail, one tap publishes (create/attach schedule under the hood) with an on-glass confirmation. This is the moat move: Canva structurally cannot own fleet publish + per-screen canvases + emergency co-tenancy.

### [P1] No multi-element align/distribute and no grouping — Canva Position-tab table stakes _(effort M)_
- **Today:** Multi-select + marquee exist (BuilderCanvas.tsx:294, 338-346, 440) and smart-guide snapping exists (snap-engine.ts:41-125), and single-zone align-to-canvas buttons exist (PropertiesPanel.tsx:821), but grep finds no alignSelected/distribute op and no groupId/createGroup anywhere in template-builder — selected elements can only be dragged together, never aligned to each other or grouped persistently
- **Canva:** Canva: Group + Position tab with align/space/distribute and Layers (canva.com/help/layer-group-align, canva.com/help/adding-and-removing-guides). OptiSigns 2.0: saves 'grouped designs' as components (Designer 2.0 post).
- **Fix:** When selection ≥2, TopContextToolbar auto-shows align-left/center/right/top/middle/bottom + distribute-h/v (pure store ops over selectedIds — small) and Cmd+G persistent groups that move/resize as one. Direct manipulation, zero settings.

### [P1] No one-click background removal — a SIGNAGE competitor now ships it, so absence reads amateur _(effort M)_
- **Today:** grep -rni 'removeBackground|background.remov' across apps/web/src and apps/api/src returns zero editor-facing hits — logos/photos with baked-in white boxes must be fixed outside the app
- **Canva:** Canva Magic Studio background remover (canva.com/newsroom/news/magic-studio). OptiSigns Designer 2.0: 'automatically detects and removes backgrounds from images' (optisigns.com Designer 2.0 post).
- **Fix:** Auto-offer a 'Remove background' chip on the context toolbar whenever an image/logo zone is selected; run client-side (@imgly/background-removal WASM, $0) with re-host to Supabase on accept. Especially potent for sponsor logos + team logos already flowing through AssetPickerField (PropertiesPanel.tsx:3331-3340).

### [P1] No magic-resize — but we uniquely KNOW every screen's real canvas, so ours should be 'Adapt to my screens', not a size picker _(effort L, MOAT)_
- **Today:** Planned-never-shipped: build log still queues magic-resize under 'big bets' (docs/research/2026-06-16-touch-editor-flagship/00-BUILD-LOG.md:118-119); grep magicResize/resizeCanvas in template-builder = 0. Meanwhile per-screen LED canvas dims exist end-to-end (tasks #23/#123: screen.canvasW/H via manifest)
- **Canva:** Canva Magic Switch 'instantly reformats all messaging, elements and contents' to any size incl. auto-translate (canva.com/newsroom/news/magic-studio — Pro-tier). OptiSigns Designer 2.0 has canvas resizing landscape↔portrait; ScreenCloud Canvas presets portrait/landscape sizes.
- **Fix:** One 'Adapt to my screens' action that reads the tenant's actual fleet canvases (1920×1080, portrait, 960×1080 ribbon, multi-panel LED) and generates a reflowed variant per size — reusing the AI-designer reflow + FitScaler machinery. Canva can never do this: it doesn't know your screens.

### [P1] Live-data superpower is invisible at design time — data-bound zones look identical to static ones _(effort S, MOAT)_
- **Today:** Data binding exists (REST/JSON + Google-Sheet CSV feeds, PropertiesPanel.tsx:1352; POS menu auto-86; sports consoles; App Library paste-first AppLibraryPanel.tsx:221-231) but nothing on the canvas or palette marks a zone as LIVE, and canvas previews show placeholder-ish content instead of the tenant's real menu/next-game/weather
- **Canva:** Competitors are creeping in: Rise Vision syncs Google Sheets to signage automatically; OptiSigns 2.0 has OptiSync data mapping ('connect spreadsheets/APIs for automatic design updates'). NOBODY has POS auto-86 + sports consoles + emergency on the same canvas. Canva: N/A — its screen output is frozen pixels.
- **Fix:** Pulsing 'LIVE' badge on every data-bound zone + palette tile, and render REAL tenant data in the editor canvas (today's actual menu prices, next scheduled game) — the design session becomes the demo. Pure display change, no settings.

### [P1] 3-candidate AI generation is no longer a differentiator — Rise Vision ships exactly that; our edge must be visible tenant-data grounding _(effort S, MOAT)_
- **Today:** AI creator generates 3 designer-HTML candidates + chat-to-edit (apps/web/src/app/[schoolId]/templates/page.tsx, apps/api/src/ai/designer-prompt.ts); tenant-data auto-grounding + brief-echo chips are landing now (treat as done) but candidate cards don't yet SHOW the operator that the board was built from their real data
- **Canva:** Rise Vision AI 'automatically creates three distinct designs' from a prompt, editable in their editor, AI credits in all tiers (risevision.com press release). Canva Magic Design generates a collection from a prompt (canva.com/magic-design).
- **Fix:** Make grounding the headline: each candidate card carries chips like 'Uses your live menu (12 items)' / 'Wired to Friday's game', and kept boards land with data-bound zones instead of frozen text — 'AI that designs a board that keeps itself current' is the sentence neither Rise nor Canva can say.

### [P2] Weaponize the Canva round-trip: competitors' most-loved feature is Canva IMPORT, and ours is unbuilt — import theirs and make it ALIVE _(effort L, MOAT)_
- **Today:** Canva Connect is Sprint-11 not-built (CLAUDE.md env table: 'Setting these alone does NOT enable a working Sign in with Canva flow'); Import 2.0 already turns PDF/images into real editable templates (task #253) at /[schoolId]/templates/imports
- **Canva:** Yodeck's Canva integration is rated 4.6/5 and is its most-praised content feature — but imports are static: 'if there's a mistake... it will need to be corrected in Canva then pushed anew' (yodeck.com/apps/canva-export + reviews). ScreenCloud and Rise Vision also ship Canva import/modify.
- **Fix:** Ship Canva Connect import that lands through the Import-2.0 pipeline as an EDITABLE, data-bindable template — 'bring your Canva design, we make it live (prices, scores, alerts)'. Turns their strongest integration into our on-ramp instead of our competitor.

### [P2] No reusable components or brand-locked team templates — ScreenCloud and OptiSigns both ship this governance layer _(effort M)_
- **Today:** Only per-zone duplicate exists (useBuilderStore.ts:530) and a per-zone locked flag (paste resets it, BuilderShell.tsx:507); no save-selection-as-component, no 'editors may replace text/images but not move layout' mode for CONTRIBUTOR role
- **Canva:** OptiSigns 2.0: 'save elements or grouped designs and reuse across projects with a single click'. ScreenCloud Canvas: 'elements of your brand can be locked so Creators and Editors can replace text or images without changing the layout' + per-template permissions (screencloud.com/apps/canvas). Canva: Brand Templates.
- **Fix:** 'Save as component' on any selection (stored as a tenant-scoped variant tile in the existing VariantPicker registry) + honor zone.locked against CONTRIBUTOR role so admins hand out fill-in-the-blanks templates — reuses RBAC that already exists, no new settings.

### [P2] No text effects (shadow/outline) — even OptiSigns ships an Effects rack; brand-grade signage headlines need pop at 8-foot distances _(effort S)_
- **Today:** StyleableField covers family/size/color/weight/align/line-height (§19 shipped, PropertiesPanel.tsx:4295-4297) but grep shows no textShadow/outline editor control anywhere in PropertiesPanel.tsx — hits at 1085-1389 are static preset configs, not operator controls
- **Canva:** OptiSigns Designer 2.0 Effects category: Shadow, Outline, Texture, Gradient, Opacity (optisigns.com Designer 2.0 post). Canva: one-tap text effects (Shadow/Lift/Hollow/Splice/Outline).
- **Fix:** Two preset chips on the text context toolbar — 'Pop' (soft shadow) and 'Outline' — applied per-field via the existing __styles channel; presets not sliders, honoring the no-new-settings rule and the typography-first mandate (redaction-bar ban).


## Lens: canvas-manipulation

The canvas is closer to Canva than expected — marquee + shift-click multi-select with group move (BuilderCanvas.tsx:320-477), labeled snap guides to zone edges/centers + canvas center (snap-engine.ts), all 8 resize handles (BuilderZone.tsx:71), cmd-D/C/V, arrow nudge with Shift/Alt steps, lock, z-order, 50-deep undo, and a shortcuts modal all exist and work. What kills the 'feels like Canva' sensation is (1) the default 5%-grid quantization that makes every drag chunky AND defeats the element-snap engine, (2) multi-select collapsing the moment you grab a resize handle (no group scale), (3) zero right-click context menu, (4) no alt-drag duplicate, (5) no shift aspect-lock / alt from-center resize modifiers, and (6) no equal-spacing/measurement guides or distribute. None of these need a new setting — they're all default-behavior and modifier-key fixes. Rotation, wheel-zoom/space-pan, and group/ungroup are the second tier. The store model (selectedIds: string[], updateZones batch patcher, beginTransaction) is already multi-select-ready for nearly all of it; only persistent grouping needs a Zone model field.

### [P0] Default grid-snap quantizes every drag to 5% steps and largely defeats the element-snap guides _(effort S)_
- **Today:** snap-engine.ts:49-52 snaps x/y to the grid whenever the grid is VISIBLE (snapGrid: showGrid passed at BuilderCanvas.tsx:390-394), and store defaults are showGrid: true + gridSize: 5 (useBuilderStore.ts:156-159, constants.ts:553). So out of the box every drag moves in 5%-of-canvas jumps (~192px on a 4K design), and because element snapping then runs on the already-quantized position with a 1.5% threshold (constants.ts:555, snap-engine.ts:71-81), a neighbor edge/center at any non-multiple-of-5 position is unreachable — the beautiful labeled guide system (BuilderCanvas.tsx:637-689) rarely fires. snapResize has the same coupling (snap-engine.ts:118-124). This is the single biggest tactile difference vs Canva: dragging feels steppy, not fluid.
- **Canva:** Canva never quantizes pointer movement to a grid by default — dragging is 1:1 fluid and magenta guides snap to neighbors/canvas as you pass them; grid/rulers are a visual overlay only.
- **Fix:** Decouple 'grid visible' from 'movement quantized': make dragging follow the pointer 1:1, run element/canvas snapping on the raw position, and treat grid lines as just another snap TARGET (within the same 1.5% threshold) instead of a pre-quantizer. No new setting — the existing Grid/Magnet toggles keep their meaning, the default just stops fighting the operator.

### [P1] Grabbing a resize handle collapses multi-select — no group resize/scale _(effort M)_
- **Today:** onResizePointerDown does select([zoneId]) unconditionally (BuilderCanvas.tsx:355-363), so the moment an operator with 4 zones selected touches a handle, the selection collapses to one zone and only it resizes. Multi-select group MOVE works (move branch uses updateZones over dragState.origs, BuilderCanvas.tsx:379-401), and the store is fully ready (selectedIds: string[] + updateZones batch patcher, useBuilderStore.ts:46,551-558) — only the resize path is single-zone.
- **Canva:** Canva draws one bounding box around a multi-selection with corner handles that proportionally scale every selected element's position and size together.
- **Fix:** When selectedIds.length > 1, render a selection bounding box with corner handles; on drag, compute the scale factor of the box and apply it to each zone's x/y/w/h relative to the box origin via the existing updateZones patcher. No UI addition beyond the box that operators already expect.

### [P1] No alt/option-drag to duplicate _(effort S)_
- **Today:** onZonePointerDown reads shiftKey/metaKey/ctrlKey only for additive selection (BuilderCanvas.tsx:336-353); altKey is never checked anywhere in the drag path, so alt-dragging a zone just moves it. duplicateZone() already exists in the store (useBuilderStore.ts:516-532) — it's only reachable via cmd-D or a button.
- **Canva:** Alt/option-drag peels off a copy in place and drags the copy — the fastest repetition gesture in every design tool, used constantly for menu rows, sponsor logos, stat tiles.
- **Fix:** In onZonePointerDown, if e.altKey: call duplicateZone(id) for each selected zone first, then start the move drag on the new copies. Pure gesture, zero UI.

### [P1] No shift aspect-lock or alt resize-from-center modifiers _(effort S)_
- **Today:** The resize branch of the drag effect (BuilderCanvas.tsx:402-418) applies raw dx/dy per handle with no e.shiftKey or e.altKey handling — a corner drag on a logo or image zone freely distorts the box, and there is no way to resize symmetrically around center. All 8 handles exist (BuilderZone.tsx:71) so the surface is ready.
- **Canva:** Canva locks aspect ratio on corner handles by default for media, shift toggles the lock, and alt resizes from center — muscle memory shared with Figma/PowerPoint/Illustrator.
- **Fix:** In the resize pointermove: corner handles on IMAGE/LOGO/VIDEO zones keep aspect by default (shift releases it; other widgets get shift = lock), alt mirrors the delta around the zone center. Modifier keys only — no setting, matches every tool the operator already knows.

### [P1] No right-click context menu anywhere on the canvas _(effort M)_
- **Today:** There is no onContextMenu handler in the entire template-builder directory (verified by grep; the canvas root at BuilderCanvas.tsx:543-559 handles onPointerDown/onDragEnter only), so right-click shows the browser's default menu. Every action it would carry already exists in the store — duplicateZone, removeSelected, moveLayer up/down/top/bottom, toggleLock, copy/paste clipboard (BuilderShell.tsx:471-514, useBuilderStore.ts:593-620) — but they're scattered across keyboard shortcuts, the bottom bar, and the Layers panel.
- **Canva:** Right-click on any element opens copy/paste/duplicate/delete/layer-order/copy-style — it's the primary discovery surface for canvas actions and a huge 'this is a real design tool' signal.
- **Fix:** Add an onContextMenu on zones and canvas that opens a small menu wired to the existing store actions (Duplicate, Copy/Paste, Delete, Bring forward/Send back/To front/To back, Lock, Copy style). Pure re-exposure of shipped actions — nothing new to build in the store.

### [P1] Snap engine has no equal-spacing hints, no pixel-distance labels, no canvas thirds _(effort M)_
- **Today:** collectTargets only emits canvas 0/50/100 plus other zones' edges and centers (snap-engine.ts:6-23) — no rule-of-thirds lines and no 'gap between A and B equals gap between B and C' spacing detection. The guide-label code explicitly acknowledges the gap: 'Canva shows pixel offsets between elements; we'd need both end positions to compute that, so percent-of-canvas is a clean v1' (BuilderCanvas.tsx:639-655) — labels show raw percentages like '37.5%', which mean nothing to an operator.
- **Canva:** Canva shows pink equal-gap badges when spacing matches between neighbors, snaps to those equal-spacing positions, and displays real distance measurements between the dragged element and its neighbors.
- **Fix:** Extend collectTargets with (a) canvas 33.3/66.6 thirds and (b) equal-gap candidates computed from pairs of aligned neighbors, and convert guide labels from percent to real pixels using meta.screenWidth/Height (already in the store). The dragging operator gets 'even spacing' for free — no toggle.

### [P1] No distribute-evenly action; single-zone align buttons buried in a collapsed panel section _(effort S)_
- **Today:** MultiAlignButtons offers Left/CenterX/Right/Top/CenterY/Bottom only (PropertiesPanel.tsx:2011-2039) — there is no distribute-horizontally/vertically for 3+ zones, so evenly spacing sponsor logos or menu rows is manual math. The single-zone align-to-canvas buttons (PropertiesPanel.tsx:821-827) sit inside the Geometry CollapsibleSection which is hidden by default (PropertiesPanel.tsx:1409-1418 'hidden by default, one click to open').
- **Canva:** Canva's Position panel has Align + 'Space evenly' (tidy up) one click away, and it also auto-tidies via the equal-spacing smart guides during drag.
- **Fix:** Add two Distribute buttons (H/V) to the existing MultiAlignButtons grid — sort selected zones by x (or y) and space the gaps equally via the existing updateZones batch patcher; show the same align/distribute cluster in the floating selection context (bottom bar) when 2+ zones are selected instead of only deep in the right panel.

### [P2] Rotation exists only as a hidden panel field — no on-canvas rotation handle _(effort M)_
- **Today:** Zone rotation shipped in the §19 wave as defaultConfig._zoneRotation applied via transform: rotate() (BuilderZone.tsx:367-380) and is edited numerically in PropertiesPanel — but the HANDLES array is the 8 resize handles only (BuilderZone.tsx:71); there is no grab-and-turn affordance, and the snap engine + marquee hit-test (BuilderCanvas.tsx:464-466) operate on the unrotated rect.
- **Canva:** Canva shows a rotate handle below every selected element with a live degree badge and snaps at 0/45/90; rotation is a direct-manipulation gesture, never a number field.
- **Fix:** Add a rotate handle beneath the selection outline that writes _zoneRotation through the existing updateZone path, snapping to 15-degree increments near cardinal angles (release with a modifier for free rotation). The persistence + render path already exists — this is only the gesture.

### [P2] No wheel/pinch zoom, no space-drag pan, no zoom keyboard shortcuts _(effort M)_
- **Today:** Zoom is only reachable via preset stepper buttons in the bottom bar (BuilderShell.tsx:1541-1570, presets 0.25-3) and setZoom in the store (useBuilderStore.ts:633); there is no onWheel handler, no pinch support, and no space-drag pan anywhere in BuilderShell/BuilderCanvas (verified by grep) — panning while zoomed relies on the overflow-auto scrollbars of the wrapper (BuilderCanvas.tsx:499). cmd+= / cmd+- / cmd+0 are absent from the keyboard handler (BuilderShell.tsx:427-529). To be fair, zoom=1 IS a fit-to-viewport baseline (BuilderCanvas.tsx:527-533), so 'fit' exists.
- **Canva:** Ctrl/cmd+scroll and trackpad pinch zoom toward the cursor; space-drag pans; cmd+0 fits, cmd+1 goes to 100%.
- **Fix:** Add ctrl/cmd+wheel (and pinch ctrlKey wheel events) zoom-to-cursor on the canvas wrapper, hold-space-to-pan, and cmd+0/cmd+= /cmd+- in the existing keyboard handler — all writing through the existing setZoom. Gestures only, no new chrome.

### [P2] No group/ungroup and no cmd-A select-all _(effort L)_
- **Today:** The Zone model has no group field (types.ts:70-90) and the store has no group actions, so operators can't bind a headline+logo+stat cluster into a unit that moves/duplicates together across edits — every re-selection is a fresh marquee. cmd-A is also absent from the shortcut handler (BuilderShell.tsx:427-529; shortcuts modal list at 1640-1656 confirms). Selection is array-based (selectedIds, useBuilderStore.ts:46) so transient select-all is a one-liner; persistent grouping needs a groupId on Zone + click-resolution to the group.
- **Canva:** cmd-G groups a selection into a unit that selects/moves/scales as one (double-click enters it), cmd-shift-G ungroups, cmd-A selects everything.
- **Fix:** Ship cmd-A immediately (select all zones on the active scene). For grouping: add an optional groupId to Zone (additive schema), make click select the whole group with double-click drilling in, and put Group/Ungroup in the new right-click menu — leaning on the already-working batch move (updateZones) so groups need no new drag code.


## Lens: editor-workflow

The editing meta-loop is solid at the surface — undo/redo is wired to Cmd-Z/Shift-Cmd-Z/Ctrl-Y with a visible bottom-bar rail and covers zones, meta, background, and brand-apply; a save-status chip with last-saved time ships; a live fullscreen preview exists; and a 'Put on a screen' express lane exists on the gallery. But the trust layer under it has real holes: (P0) editing a starter/system template then hitting Back or refresh destroys the draft silently — both the dirty-check and beforeunload are explicitly skipped for isSystem, on the exact path new operators take; undo commits a full snapshot per KEYSTROKE into a 50-entry buffer, so typing one headline erases the whole undo trail and Cmd-Z walks back a character at a time; the fullscreen preview drops per-field text styles/rotation/opacity and stacks all kiosk scenes on top of each other (the canvas and player both do this correctly — preview is the odd one out); there is no version history of any kind (zones-save is a destructive deleteMany+recreate) and no two-tab conflict detection, so one stale Save silently wipes another session's work; the builder itself has no publish action (the express lane is gallery-only, making the most common exit intent a 5-step, 3-surface trek); and template resize is a naive stretch — the researched magic-resize 'big bet' never shipped, though VenueOS could do the fleet-aware version Canva structurally can't. Fix cluster 1 (data safety: system-draft prompt, local draft journal, server snapshots, 409 conflict) plus fix cluster 2 (undo coalescing, preview unification, in-builder publish) would put the workflow loop decisively ahead of Canva for signage.

### [P0] Starter/system template drafts are silently destroyed on Back or refresh — zero prompt, zero recovery _(effort S)_
- **Today:** BuilderShell.tsx:382 — handleBack short-circuits for system templates (`if (template.isSystem) { onBack(); return; }`) BEFORE the isDirty check, and BuilderShell.tsx:544 skips the beforeunload guard for isSystem too. Meanwhile the banner at BuilderShell.tsx:662-668 tells the operator 'Edit anything you like — then click Save to my templates.' System presets are the primary entry path for new operators ('Use' from the gallery opens a draft), so a first-session user who edits a starter board for 20 minutes and hits Back (or Cmd-R) loses everything with no warning. All draft state is in-memory Zustand only — nothing in localStorage.
- **Canva:** Canva never loses work: continuous autosave from the first keystroke on any template, including starter templates (it forks a copy implicitly the moment you edit).
- **Fix:** Keep the silent exit ONLY when the draft is clean. When isDirty on a system preset, Back prompts 'Keep your edits?' with one-click 'Save to my templates' (handleCustomize already exists at BuilderShell.tsx:362 — direct reuse) / 'Discard'. Re-enable beforeunload for dirty system drafts. Zero new settings; the existing fork path just stops being skippable by accident.

### [P1] Undo commits one history entry PER KEYSTROKE — typing a headline wipes the entire 50-deep undo trail _(effort M)_
- **Today:** TextField/TextAreaField fire onChange on every keystroke (PropertiesPanel.tsx:7263, 7287) → ContentFields.setField calls updateZone(..., commit=true) (PropertiesPanel.tsx:2218-2220) → each keystroke pushes a full deep-copy snapshot of ALL zones + meta (useBuilderStore.ts:547) into a HISTORY_LIMIT=50 buffer (useBuilderStore.ts:5). Typing a 60-char headline = 60 snapshots: the operator's prior layout work falls off the end of history, and Cmd-Z then walks back one character at a time. Arrow-key nudges also commit per key-repeat (BuilderShell.tsx:516-524), so holding an arrow for 2s burns ~30 entries. setMeta has the same per-keystroke commit on template name/description (PropertiesPanel.tsx:993, 1003; useBuilderStore.ts:560-565).
- **Canva:** Canva coalesces a continuous typing session into ONE undo step and keeps a much deeper effective history; undo after typing reverts the whole text edit, not one character.
- **Fix:** Coalesce automatically: push the history snapshot on field FOCUS (start of an edit session) instead of per change — TextField/TextAreaField already track local state, so setField can pass commit=false during typing and the panel commits once on focus via beginTransaction (already exists, useBuilderStore.ts:666). Same for held arrow-keys (commit on first keydown of a burst, debounce ~500ms). No setting; undo just starts behaving like every design tool.

### [P1] Fullscreen Preview lies: per-field text styles, rotation/opacity are dropped, and multi-scene kiosks render all scenes stacked on top of each other _(effort M)_
- **Today:** TemplatePreviewModal.tsx:168-193 maps raw zones into bare WidgetPreview — it never builds the per-field `_styles` CSS rules, `_zoneRotation`, or `_zoneOpacity` that BOTH the builder canvas (BuilderZone.tsx:369-378, 643-717) and the real player (player/page.tsx:5850-5905) apply, so any font/color/size the operator set via StyleableField or the bottom bar simply vanishes in the 'what will this look like on TV' check. It also has zero sceneId filtering, while the canvas filters (BuilderCanvas.tsx:288-291) and the player filters (player/page.tsx:5631-5633) — so a multi-scene kiosk previews as every scene overlaid. The gallery's own TemplatePreviewModal (templates/page.tsx:2453, zones passed raw at ~2528) has the same two defects. Ironically the AI candidate preview DOES scene-filter correctly (templates/page.tsx:2321-2325).
- **Canva:** Canva's Present/Preview renders pixel-identically to the editor and to the published artifact — one render path.
- **Fix:** Extract the player's zone-render block (scene filter + _buildPlayerRules CSS + rotation/opacity) into one shared component and use it in canvas preview, both preview modals, and the player. Preview-vs-reality mismatch is the classic silent trust-killer: the operator styles text, previews, sees the styling gone, and concludes the editor is broken. For multi-scene templates, add scene prev/next arrows in preview (mirrors the candidate preview UX that already shipped).

### [P1] No version history anywhere — one bad Save (or brand-apply, or AI chat-edit) permanently destroys the previous design _(effort M)_
- **Today:** schema.prisma has Template (line 1139), TemplateZone (1241), TemplateScene (1287) — no TemplateVersion/snapshot model. PUT /templates/:id/zones is a destructive atomic replace: deleteMany + recreate (templates.controller.ts:2262-2289) with no prior-state capture; AuditLog records only changed field KEYS (templates.controller.ts:2200-2206). Undo history is in-memory and dies on tab close. So 'restore what this board looked like yesterday' is impossible — a mis-fired Apply-brand-to-all-zones or an AI refine that got saved is unrecoverable.
- **Canva:** Canva keeps full automatic version history on every design (browse + restore any prior state, free tier included).
- **Fix:** Lightweight server-side snapshots, fully automatic: on each zones-replace, first copy the current zone set + meta into a template_snapshots JSONB row (keep last ~20, prune older). Gallery card menu + builder toolbar get 'Restore a previous save' with visual thumbnails (ScaledTemplateThumbnail already renders zone arrays). No settings, no migration risk — additive table.

### [P1] No publish path from inside the builder — 'Put on a screen' exists only back on the gallery card _(effort S, MOAT)_
- **Today:** BuilderToolbar's entire action set is Back/Preview/flip/Save/Save-as/Discard (BuilderToolbar.tsx:12-27) — no route to a screen. The express lane (create one-item playlist → hand off to the playlists Publish-to-Screens sheet) exists but only as a gallery-card action (templates/page.tsx:921-951, button at 3070-3082). So the natural flow — finish editing, get it on the wall — is: Save → Back → scroll/find the card → Put on a screen → land on a different page's publish sheet. ~5 steps across 3 surfaces, violating the 30-second happy-path gate for the single most common editor exit intent.
- **Canva:** Canva puts Share/Publish in the editor header — but Canva CANNOT publish to a physical screen fleet at all; this is VenueOS's endgame action.
- **Fix:** Reuse the exact express-lane handler as a builder-toolbar action: after a successful Save, the SaveStatusChip area shows 'Saved ✓ — Put on a screen' which runs handlePutOnScreen(templateId) directly (the handler is already role-guarded and phone-friendly). One click from editor to the publish sheet. Bonus auto-detect: if the template is already live in playlists on screens, show 'Live on N screens — changes are already up' instead, closing the 'did my edit reach the wall?' anxiety loop.

### [P1] Template resize is a naive stretch — orientation flip distorts every zone, and the planned magic-resize never shipped _(effort L, MOAT)_
- **Today:** flipCanvas (useBuilderStore.ts:622-631) swaps screenWidth/screenHeight and touches nothing else — every zone keeps its %-geometry, so a 16:9 layout flipped to 9:16 squashes each widget to a different aspect and px-based font sizes stop fitting. 'Adapt for LED' (templates/page.tsx:2600+, duplicate call at 2005) duplicates at the new canvas with the same raw % zones — same stretch. The flagship research named auto-reflow across landscape/portrait/ribbon as THE high-leverage Canva gap (docs/research/2026-06-16-touch-editor-flagship/02-COMPETITIVE-AI-LANDSCAPE.md:483, 148), and 00-BUILD-LOG.md:118 still lists magic-resize under un-started 'big bets'; zero implementation hits for magicResize in apps/.
- **Canva:** Canva Magic Switch re-flows a design into any target size — repositions and re-typesets elements rather than stretching.
- **Fix:** Signage-native magic resize that Canva structurally can't copy: the system already KNOWS the tenant's fleet (every paired screen's resolution + canvasW/H). One click on 'Adapt for your screens' generates variants for the ACTUAL sizes in the fleet — landscape TV, portrait kiosk, 320×1080 ribbon — using per-archetype reflow (row-of-zones → column, font auto-fit via the existing FitScaler work) and opens each for tweak. Auto-detect targets, no size form.

### [P2] Two tabs / two operators editing the same template: last Save silently wipes the other's work (no conflict detection) _(effort S)_
- **Today:** PUT /templates/:id (templates.controller.ts:2154-2208) and PUT /templates/:id/zones (2216-2289) accept writes unconditionally — no updatedAt/If-Match/version token. The builder loads the template once into Zustand (BuilderShell.tsx:94-141) and handleSave (161-222) pushes the full zone set back, so a stale tab's Save atomically deletes and replaces everything the other session saved (deleteMany at 2263). Greg runs the product from an iPhone AND a desktop — the two-device case is his own daily workflow. Failure is 100% silent: both saves return 200.
- **Canva:** Canva does real-time multiplayer editing; even its degraded mode detects concurrent edits rather than silently discarding them.
- **Fix:** Auto-detect, zero settings: builder keeps the template's updatedAt from load; both PUTs compare it and 409 on mismatch; the builder catches the 409 and shows 'This template was changed (on your phone / by Sam) since you opened it — Reload their version / Save mine as a copy' (Save-as-copy already exists). Pairs naturally with the version-snapshot finding so neither side's work is ever lost.

### [P2] Scene create/delete persists to the server INSTANTLY and is exempt from undo — the opposite of every other builder action _(effort M)_
- **Today:** ScenesPanel createScene.mutateAsync (ScenesPanel.tsx:85) and deleteScene.mutateAsync (ScenesPanel.tsx:128) write to the API immediately, while zones/meta wait for explicit Save; the store deliberately excludes scenes from history (useBuilderStore.ts:40-43), so Cmd-Z cannot restore a deleted scene, and Back-without-saving doesn't roll it back either — deletion already moved that scene's zones server-side. One mixed persistence model inside one editor: 'nothing sticks until Save' is true for everything except the most destructive action in the kiosk workflow.
- **Canva:** Canva pages (its scene equivalent) are fully undoable — delete a page, Cmd-Z brings it back with all content.
- **Fix:** Make scene delete soft in the editor: mark the scene (and its zone assignments) deleted in the store, include it in the undo snapshot, and apply the actual API delete on Save alongside the zones flush (the save path already recreates scenes on save-as, BuilderShell.tsx:281-341, so the plumbing exists). Undo then covers EVERY mutation with one consistent rule and no confirmation dialogs needed.

### [P2] No crash/reload recovery for in-progress edits — autosave was disabled outright instead of redirected to a local draft _(effort M)_
- **Today:** AUTO_SAVE_IDLE_MS=15_000 is defined (BuilderShell.tsx:55) but unreferenced — autosave was deliberately disabled because server-persisting on idle made changes 'stick' without explicit Save (BuilderShell.tsx:372-374). The replacement is only beforeunload (BuilderShell.tsx:531-553), which covers graceful closes but not browser crashes, iOS Safari tab eviction (Greg's iPhone workflow), battery death, or the stale-bundle hard reloads this app has shipped. All draft state lives in in-memory Zustand; nothing is journaled locally, so any of those events destroys the session's work.
- **Canva:** Canva autosaves continuously to the server; a crash costs at most a couple of seconds of work.
- **Fix:** Respect Greg's explicit-Save semantics but journal the draft LOCALLY: debounce-write {templateId, zones, meta, ts} to IndexedDB on change, clear it on successful Save/Back-discard, and on next open of the same template show a one-tap 'Restore unsaved work from 3:41 PM?' banner. Server state stays untouched until Save — no new setting, no behavior change in the happy path, crash-loss goes to zero.

### [P2] Renaming a template requires opening the full builder — no inline rename in the gallery _(effort S)_
- **Today:** GalleryCard's action set is Use/Edit/Put-on-a-screen/Duplicate/Export/Adapt-for-LED/Delete/Preview (templates/page.tsx:2832-2848) — no rename. The only rename path is builder → Properties → Template name field (PropertiesPanel.tsx:993), where each keystroke also pushes an undo snapshot via setMeta (useBuilderStore.ts:560-565), then Save. Duplicates auto-name as '<name> copy' (BuilderShell.tsx:226), so gallery clutter of 'copy'/'copy copy' names is expensive to clean up.
- **Canva:** Canva: click the title text anywhere (dashboard tile or editor header) and type — instant inline rename.
- **Fix:** Make the name text on the gallery card and the builder toolbar directly editable (click-to-edit inline input, save on blur via the existing useUpdateTemplate metadata PUT). Direct manipulation, one field, no dialog, no setting.


## Lens: elements-assets

VenueOS's palette is strong on FUNCTIONAL widgets (400+ live-preview variant tiles, vertical filtering, drop-at-cursor physics, quick layouts, App Library smart placement) but has almost no answer to Canva's Elements/Photos tray: no static shapes or lines, no operator icon library beyond typed emoji, and — the headline — a production-grade Pexels stock-photo service (stock-image.service.ts) that only the AI pipeline can call while every operator-facing picker (AssetPicker, AssetPickerField, BackgroundPanel) offers upload-or-nothing. Three of the nine findings are near-free wins on already-shipped code: expose stock search in the asset picker (S — endpoint + picker tab over the existing service and rehost path), resurrect the 8 finished-but-unregistered Decoration elements (S — dead constants.ts registry, classic rule-#9 class), and add canvas-level file drop (S — today a missed drop navigates the tab away and unmounts the builder). Shapes (M) and a lucide-backed icon widget (M) are the two genuine build items. None of these are moat features — they're the parity floor Canva sets for 'feels easier than Canva'; the moat stays the live-data widgets these elements would decorate.

### [P0] Pexels stock-photo search is fully built server-side but invisible to operators — no in-editor photo search anywhere _(effort S)_
- **Today:** StockImageService.search() (apps/api/src/ai/stock-image.service.ts:74-116) is production-grade — key-in-header, 6s AbortSignal, orientation bias, never-throws — but its ONLY callers are the AI board pipeline (art-director.ts:717, ai.service.ts attachKeptBoardPhoto). ai.controller.ts exposes only /generate, /text/rewrite, /edit/resolve, /image (lines 95-161) — no stock-search endpoint. Operator-facing pickers offer zero photo search: AssetPicker.tsx:250-262 = folder browse + 'Upload new' only; AssetPickerField (PropertiesPanel.tsx:8333-8398) = URL input + 'Browse library'; BackgroundPanel.tsx = brand colors/12 gradients/6 patterns/upload. Operator must leave the app, hunt a photo, download, re-upload.
- **Canva:** Photos tab in the side tray — search millions of stock photos, click/drag one straight onto the canvas. It is the single most-used tray after Text.
- **Fix:** Thin GET /api/v1/ai/stock-search?q&orientation controller over the EXISTING service (return top ~12 {thumbUrl,url,photographer}), plus a 'Find a photo' search input inside AssetPicker/AssetLibraryModal — results render in the same grid, pick = server re-hosts into Supabase via the already-shipped Pexels rehost path (templates.controller.ts:1458 rehostStockImages + :2405 host-allowlist) so it becomes a normal Asset. Zero new deps, zero settings; feature invisible when PEXELS_API_KEY unset (service already returns null). This is the highest-leverage item in the whole lens: ~1 endpoint + 1 picker tab closes Canva's biggest tray advantage for $0.

### [P1] No static SHAPE widget — rect / circle / line / arrow / divider don't exist as elements _(effort M)_
- **Today:** No SHAPE widget type anywhere: grep of variants-register.ts and WidgetRenderer.tsx returns nothing. Closest stand-ins are (1) TOUCH_POINT circle/square/arrow variants (variants-register.ts:890-922) — interaction hotspots with default bgColor:'transparent' and tap semantics on the player, not design shapes; (2) animated DecorationWidget (motion effects, not static). There is no line, no divider, no filled rectangle with border controls; zones also carry no rotation field (types.ts), so even a hacked TEXT-as-box can't make a diagonal accent.
- **Canva:** Elements tray: lines & shapes as first-class draggables with fill, border color/weight/style, corner radius, rotation — the building blocks of nearly every Canva design.
- **Fix:** One ShapeWidget file (config.shape: rectangle|pill|circle|line|arrow|triangle|star as SVG), PropertiesPanel case with fill + border color/width + radius + opacity using the existing ColorPickerField brand-swatch presets (brand primary/accent one tap), and ~7 registerVariant tiles under a new 'Elements' chip in VariantPicker. Renders as plain SVG = Taurus-safe. No settings, drag-in like any tile.

### [P1] The 8 Decoration elements (confetti, ribbon, balloons, sparkles, neon…) are built and editable but UNREACHABLE — dead palette registration _(effort S)_
- **Today:** DecorationWidget.tsx is complete (APPROVED 2026-04-27, 8 variants, DECORATION_VARIANTS metadata at lines 400-414), WidgetRenderer.tsx:568 dispatches DECORATION, PropertiesPanel.tsx:3967 has its editor case, useBuilderStore.ts:408-439 canonicalizes DECORATION_* types — but the only palette listing is constants.ts WIDGET_GROUPS:307-321, which NO rendered surface imports (grep: consumers import only getZoneColor/widgetLabel/widgetIcon/validateTouchHitTargets; VariantPicker and templates/page.tsx each define their OWN local WIDGET_GROUPS). variants-register.ts has zero DECORATION registrations → the tiles never appear in the operator's Widgets panel. This is exactly the CLAUDE.md rule-#9 WidgetPalette dead-file class.
- **Canva:** Ships confetti/sparkle/animated 'stickers' prominently in the Elements tray; they're a signature delight feature.
- **Fix:** 8 mechanical registerVariant() calls in variants-register.ts sourcing name/hint/defaults straight from the exported DECORATION_VARIANTS array (widgetType:'DECORATION', defaultConfig from .defaults). ~30 lines, instantly resurrects a finished feature.

### [P1] Dropping a file that misses an image zone navigates the tab to the file — builder unmounts; no canvas-level file drop _(effort S)_
- **Today:** Native file drop is accepted ONLY on zones of type STAFF_SPOTLIGHT/LOGO/IMAGE_CAROUSEL/IMAGE (BuilderZone.tsx:155-177, stopPropagation inside). BuilderCanvas.tsx's only onDrop is a dnd-kit hover reset (line 557); grep finds NO document/window-level dragover/drop preventDefault anywhere in template-builder. Browser default for an unhandled file drop = replace the tab with the image file → the whole builder unmounts mid-edit. Also single-file only (dataTransfer.files?.[0] at BuilderZone.tsx:176).
- **Canva:** Drop an image anywhere on the canvas → uploads and places it at the cursor at sensible size; multi-file drop creates one element each; a stray drop never destroys the session.
- **Fix:** Canvas-level onDragOver/onDrop on BuilderCanvas: upload via the exact code BuilderZone already has, then addZone('IMAGE', dropAtCursor) with the asset seeded — plus a window-level dragover/drop preventDefault while the builder is mounted as the safety net. Accept multiple files → one IMAGE zone each. Pure friction-removal, no UI added.

### [P1] No operator-facing icon library — icons are 'type an emoji into a text field' _(effort M)_
- **Today:** lucide-react is bundled and used across all app chrome (e.g. PropertiesPanel.tsx:5) but no ICON widget exists (grep 'ICON' in variants-register.ts / WidgetRenderer.tsx: none). Operator icon affordances are raw emoji TextFields: PropertiesPanel.tsx:2713 'Icon (emoji or short text)', :4227 'Food emoji (🍕 🌮…)', :3680 'Mascot emoji'. Emoji render inconsistently across the player fleet (Android WebView vs desktop) and can't take brand colors.
- **Canva:** Searchable icons/graphics in the Elements tray, one-click recolor to brand palette.
- **Fix:** ICON widget backed by lucide's dynamic-icon entry point (already in the dependency tree — ~1500 names, no new dep): searchable picker modal (fuzzy match on icon name), color defaults to var(--brand-primary), stroke-width + size auto-fit the zone. SVG stroke = crisp at LED scale and identical on Chromium 83.

### [P2] AI image generation is stranded on the /assets page — absent from the picker where the need actually arises _(effort S)_
- **Today:** AiImageGenerateButton (components/ai/AiImageGenerateButton.tsx) is mounted exactly once: assets/page.tsx:660. Inside the builder, AssetPicker.tsx and AssetLibraryModal (PropertiesPanel.tsx:8625) offer only browse + upload — an operator mid-design must leave the editor, navigate to Assets, generate, return, and re-open the picker to use it.
- **Canva:** Magic Media generates images inside the editor tray; the result drops straight into the design.
- **Fix:** Mount the existing AiImageGenerateButton in the AssetPicker/AssetLibraryModal toolbar next to 'Upload new' — it already takes an onGenerated(asset) callback, so wiring it to onPick(asset.fileUrl) auto-places the result. Its own gating already hides it when no provider is configured. ~20 lines.

### [P2] Palette drops ignore the shipped smart-size map — everything lands as the same 40×30% box under a generic 'Drop to Add' ghost _(effort S)_
- **Today:** Drop-at-cursor physics EXIST and work (BuilderShell.tsx:610-627 resolves the drop to canvas-% and centers the zone; useBuilderStore.ts:311-313). But useBuilderStore.ts:287-298 documents that the size param is passed ONLY by the App Library — 'the plain Widgets palette… NEVER passes size' — so a LOGO, QR, TICKER, and full-bleed IMAGE all land 40×30. The keyed fallback map already exists (AppConfigForm.tsx:174 WIDGET_TYPE_DEFAULT_SIZE). The DragOverlay ghost is a generic indigo box (BuilderShell.tsx:766-772) even though VariantTile already renders live widget previews.
- **Canva:** Dragged elements drop at their natural aspect/size and the drag ghost IS the element, so placement is WYSIWYG.
- **Fix:** Export WIDGET_TYPE_DEFAULT_SIZE (move to constants.ts) and thread it through BuilderShell.handleDragEnd + VariantPicker.handlePick as the size arg addZone already accepts; render the dragged variant's live tile in DragOverlay instead of the indigo box. No behavior settings, just better defaults.

### [P2] Background panel's photo lane is upload-only — hand-built templates can't get the stock photos AI boards get automatically _(effort S)_
- **Today:** BackgroundPanel.tsx ships three lanes (brand colors, 12 curated gradients + 6 SVG patterns, custom) with custom delegating to CanvasBackdropSection (PropertiesPanel.tsx:7526) = solid color + raw-CSS gradient field + file upload. Meanwhile AI-designer boards are photo-forward by default via the same Pexels service — a manual template's backdrop has no path to a real photo without leaving the app.
- **Canva:** Background tab includes photo search; one click sets a stock photo as the canvas background.
- **Fix:** Piggyback on the stock-search endpoint from finding #1: add a 'Photos' row to BackgroundPanel that searches Pexels inline and sets meta.bgImage with the re-hosted URL (same onChange({bgImage}) path CanvasBackdropSection uses at :7577). One component, shared endpoint.

### [P2] SVG uploads are hard-blocked, so vector logos/graphics can't enter the asset pipeline at all _(effort M)_
- **Today:** AssetPicker.tsx:107-111 rejects SVG with 'export as PNG (SVG support is coming soon)'; the accept list (lines 91-98) omits image/svg+xml. Reason is legit (direct browser→Supabase presign can't sanitize a stored-XSS SVG, per the comment at :86-90), but the result is every crisp vector logo — the exact asset class a shapes/icons story depends on — gets rasterized before it reaches a 4K LED wall. Task #223 (scraped SVG logo falls back to raster) is the same root gap.
- **Canva:** Full SVG upload support; vectors stay vectors at any export size.
- **Fix:** Route SVG uploads through the server (POST /assets/upload already exists and takes multipart) where a sanitizer pass (DOMPurify/jsdom or svgo with script/foreignObject/event-attr stripping) runs before the Supabase write; keep the presign path PNG/JPEG-only. Auto-detect by mime — no operator-visible change except SVGs now just work.


## Lens: inline-text-editing

The React-zone architecture is already Canva-grade in structure and must NOT be re-flagged: one-click on-canvas contentEditable editing shipped (BuilderZone.tsx enterFieldEdit :222-350 with dotted-path commit, drag-vs-edit threshold, Escape revert), 109 widget files expose data-field hotspots, and a persistent bottom bar (BuilderShell.tsx BuilderBottomBar :831+) gives per-field font/size/B-I-U-S/color synced two-way with canvas clicks and panel focus via the template-edit-field event, plus Cmd+B/I/U shortcuts and a field/zone/template scope selector. The AI-designer srcdoc path also verified: typing in the panel live-updates the board per keystroke via educms-overrides postMessage with no reload (WidgetRenderer.tsx:3828-3847) — that's what the phase-4b demo was. SINGLE BIGGEST CHANGE PER ARCHITECTURE: (1) React zones — make the font system real: ~13 of the pickers' fonts are never loaded so picking them silently renders OS fallback (the #1 'editing feels broken' signal), then one searchable in-face font menu with brand fonts pinned; (2) EXTERNAL_HTML boards — kill the per-keystroke iframe remount (key={srcWithOverrides} reloads the full 4K document on every character typed; switch live edits to the postMessage path the shim already listens for — the srcdoc path proves it works); (3) AI-designer boards — in-place contentEditable typing via a shim V7 (the shim already owns the DOM inside the sandbox and the outbound message channel; today it only reports clicks). Word-level rich formatting (RICH_TEXT is a plain-text costume) and shadow/outline legibility effects are the two genuine capability gaps vs Canva; everything else is polish on an already-shipped foundation.

### [P0] Every keystroke fully reloads the board — URL-based EXTERNAL_HTML editing is remount-per-character _(effort S)_
- **Today:** apps/web/src/components/widgets/WidgetRenderer.tsx:3884-3886 renders the ~107 EXTERNAL_HTML boards with key={srcWithOverrides} src={srcWithOverrides} ('key on the full overrides URL so any edit reloads the frame'). srcWithOverrides (3738-3786) base64-bakes textOverrides into the URL, and the panel's TextField fires onChange on every keystroke (PropertiesPanel.tsx:7263 → setOverride:7024 → setField). Chain has zero debounce: one character typed → new config → new URL → React remounts the iframe → the full 3840×2160 document re-fetches, re-runs fonts/auto-fit/animations, and flashes. The srcdoc AI-designer path already solved this — postDesignerOverrides (WidgetRenderer.tsx:3828-3847) pushes the SAME educms-overrides postMessage the shim listens for, updating text live with no reload.
- **Canva:** Canva text editing is in-place and zero-flicker; the canvas never reloads while you type.
- **Fix:** Make the URL path do what the srcdoc path already does: on config change, postMessage {type:'educms-overrides', text, textStyles, img} into the live iframe (the V5/V6 shim's message listener already applies it — inject-shim-v2.cjs:135), and drop textOverrides/textStyles/img from the iframe key so typing never remounts. Keep URL params only for first paint and the player. Zero new UI, pure friction removal — typing feels instant like the AI boards do today.

### [P1] No in-place typing on EXTERNAL_HTML or AI-designer boards — shim reports clicks but can't take a keystroke _(effort M)_
- **Today:** The shim's armEdit (apps/web/scripts/inject-shim-v2.cjs:130 and the verbatim EDUCMS-SHIM-V6 copy in apps/api/src/ai/designer-edit-shim.ts:15) only does hover-outline + click → postMessage educms-field-click; the panel scrolls/focuses the matching input (PropertiesPanel.tsx:6948-6967) and ALL typing happens in the side panel. The shim runs inside the allow-scripts sandbox with full DOM access — it could flip the clicked [data-field] to contentEditable exactly like BuilderZone.enterFieldEdit already does for React zones (BuilderZone.tsx:234-350), but no such path exists. Memory's 'typed into a live board's field' (phase 4b) was typing in the PANEL with live board update — verified: that shipped; on-board typing did not.
- **Canva:** Double-click text → cursor appears ON the design → type. This is Canva's core magic and the benchmark Greg is comparing against.
- **Fix:** Shim V7: on click in edit mode, set contentEditable on the [data-field] node in-place (caret on the board, text selected), commit on blur/Enter via a new outbound message {type:'educms-field-input', key, value} that the panel writes into textOverrides; Escape reverts; panel remains as fallback + styling home. One source (inject-shim-v2.cjs + designer-edit-shim.ts share the block), then re-run the injectors per the CLAUDE.md redesign invariant. This is THE single biggest Canva-parity change for architectures 2 and 3.

### [P1] Font pickers sell ~13 fonts that are never loaded — picking them silently renders an OS fallback _(effort S)_
- **Today:** BOTTOM_BAR_FONTS (BuilderShell.tsx:821-826) offers Roboto, Open Sans, Lato, Raleway, Nunito, Source Sans Pro, Merriweather, Bebas Neue, Pacifico; FONT_OPTIONS (PropertiesPanel.tsx:7491-7509) adds Permanent Marker, Indie Flower, EB Garamond, VT323, Press Start 2P. Globally loaded fonts are ONLY Inter/Fredoka/Caveat (app/layout.tsx:2-32); the Google-Fonts set at WidgetRenderer.tsx:1433-1458 injects only when a signage-design board renders (useSignageFonts, called at :1731) and is a different list. Result: operator picks 'Pacifico — Script' for a TEXT widget → canvas AND player show generic cursive/system font → 'the font picker does nothing.' This is the exact un-loaded-font failure the 2026-06-27 'Dodgers vs Giants' bug documented for AI boards (WidgetRenderer.tsx:1417-1425), still live for the manual pickers.
- **Canva:** Every font in Canva's picker is loaded on demand; what you pick is always what renders.
- **Fix:** Auto-load on pick: one ensureFontLoaded(family) helper that injects a fonts.googleapis css2 link the first time any picker sets a family (and on template load for saved fontFamily/_styles values, builder + player). Unify the three divergent lists (BOTTOM_BAR_FONTS / dead TOOLBAR_FONTS / FONT_OPTIONS / V2_FONT_OPTIONS) to one loaded catalog. No setting — the picked font just works.

### [P1] Per-field size stepper anchors at 16px inside iframe boards — tapping '+' shrinks a 120px headline to 18px _(effort S)_
- **Today:** BuilderShell.tsx:1034-1043 measureFieldFontSize queries [data-field] in the PARENT document, but EXTERNAL_HTML and AI-designer fields live inside a null-origin sandboxed iframe — unreachable, so it returns null and the stepper falls back to 16 (BuilderShell.tsx:1145 '(curFieldStyle.fontSize ?? fieldMeasured) || 16'). Operator clicks a board headline (designed at 100-300px), taps '+' in the bottom bar → _styles[field].fontSize = 18 → the shim applies 18px (designer-edit-shim.ts applyTextAndStyles) and the headline collapses. This exact bug class was already fixed for React zones after the operator hit it ('i selected large text and hit larger and it made it tiny' — PropertiesPanel.tsx:7858-7868) but the fix can't see into iframes.
- **Canva:** Canva's size field always shows the element's real current size; +/- steps from it.
- **Fix:** Have the shim self-report: include getComputedStyle(el).fontSize (plus color/family) in the educms-field-click payload — the shim runs inside the iframe and can measure freely. Panel stores it as the anchor for the stepper. One field added to an existing message; no UI change.

### [P2] Font picker is a bare native <select>: no search, no previews on Safari/macOS, no brand fonts pinned, no recents _(effort M)_
- **Today:** All three live pickers are native selects with option-level style={{fontFamily}} (BuilderShell.tsx:1128-1140 zone path, :1305-1320 per-field path; FontFamilyField PropertiesPanel.tsx:7833-7851). macOS Safari/WebKit ignores per-option font styling in the native popover (the CLAUDE.md cross-browser rule's exact 'works in Chrome' class), so on the operator's own Mac the preview-in-own-face doesn't render at all. No search, no recently-used, no brand-kit fonts pinned at top even though the brand pipeline carries fontDisplay/fontBody (designer-edit-shim.ts BRAND_MAP fontDisplay/fontBody).
- **Canva:** Searchable font list with in-face previews and Brand Kit fonts pinned at the top — one of Canva's most-praised micro-UX details.
- **Fix:** One shared FontMenu popover (styled listbox, not <select>): type-to-filter, each name rendered in its own loaded face, 'Your brand' section pinned on top sourced from the tenant BrandKit, 5 most-recent below it. Replaces all three call sites; rides finding #3's loader so previews are real. Direct-manipulation, zero settings.

### [P2] RICH_TEXT is TEXT in a costume — no word-level formatting anywhere in the product _(effort L)_
- **Today:** WidgetRenderer.tsx:520 'case RICH_TEXT: return <TextWidget/>' — the same plain-string renderer as TEXT ({content} at ~:1380-1404, no HTML/markdown parsing). Every styling control in all three surfaces (BuilderBottomBar per-field _styles, TopContextToolbar scopes, panel fields) applies to a whole field or zone; there is no way to bold ONE word inside a headline. The inline contentEditable commit (BuilderZone.tsx:293 innerText) is plain-text by design, so even if markup existed it would be stripped on the first in-place edit.
- **Canva:** Select any word inside a text box → bold/color just that word from the floating toolbar. Table-stakes for 'easier than Canva'.
- **Fix:** Minimal rich runs for TEXT/RICH_TEXT: allow b/i/u/color spans in content (sanitized whitelist), render via a tiny run-parser in TextWidget, and in the in-place editor make the existing Cmd+B/I/U shortcuts (TopContextToolbar.tsx:144-162) apply to the current SELECTION instead of the whole field, committing innerHTML-lite instead of innerText. Player parity is the same parser. No new panels.

### [P2] No text legibility effects — shadow/lift/outline missing from the per-field styling pipe _(effort M)_
- **Today:** The per-field _styles schema starts and ends at color/fontSize/fontWeight/fontStyle/fontFamily/textDecoration/textAlign/backgroundColor/lineHeight (shim applyTextAndStyles in designer-edit-shim.ts:15; BuilderShell setFieldStyleProp:1015-1030; BottomToolbar/BuilderBottomBar UIs). No textShadow or text-stroke anywhere an operator can reach. For signage read at 8+ feet over photos, shadow/outline is the legibility tool — and it's the approved alternative to the solid-fill 'redaction bar' backgrounds Greg explicitly banned (2026-06-30 typography memory), yet backgroundColor IS in the schema and shadow is not.
- **Canva:** Effects menu: Shadow, Lift, Hollow, Outline, Glow, Curve — heavily used on photo-backed designs.
- **Fix:** Three one-tap effect chips (Shadow / Lift / Outline) on the per-field row of BuilderBottomBar, writing textShadow / -webkit-text-stroke through the existing _styles pipe (BuilderZone CSS injection + shim + player all get it for free once the schema knows the keys — both properties are Chromium-83-safe). Skip curve/echo/splice — signage doesn't need them. Chips, not a settings panel.

### [P2] Dead BottomToolbar.tsx duplicate invites the WidgetPalette bug class _(effort S)_
- **Today:** apps/web/src/components/template-builder/BottomToolbar.tsx (330 lines, BottomToolbarConnected, TOOLBAR_FONTS) is never mounted — grep shows only its own test file and a comment in BuilderShell.tsx:756; the live bar is BuilderBottomBar inside BuilderShell.tsx:831. The dead file writes zone-level config only (no per-field _styles support), duplicates the font list, and has passing tests — exactly the 'imported but never rendered' trap from CLAUDE.md rule #9 (WidgetPalette, 3 wasted edit rounds). The next agent asked to 'improve the floating text toolbar' will edit it, ship green CI, and change nothing on screen.
- **Canva:** N/A
- **Fix:** Delete BottomToolbar.tsx + its test (or fold anything unique into BuilderBottomBar) and point the BuilderShell comment at the real component. Pure de-risking; no operator-visible change.

