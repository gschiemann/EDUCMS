# Deep Wave 2 — Emergency render-fit + AI magnificence + broad audit (2026-06-27)

Workflow wf_ad074da8-079 (3 agents, ? tok). Lanes 1 (emergency) + 2 (AI design) SHIPPED to master (cherry-picked + CI-watched); Lane 3 is the read-only punch-list for the NEXT wave.

## LANE 1 — LIFE-SAFETY: emergency readability + completeness on every canvas
**SHIPPED** branch=worktree-wf_ad074da8-079-1 sha=b48028b153

- **[P0][FIXED] BULLETPROOF FALLBACK emergency message clips long text inside its zone on any canvas**  
  ↳ The synthetic fallback template (used when no emergency playlist is assigned/empty) has one TEXT zone at 100%x100% with the legacy TextWidget renderer, which clamps fontSize to Math.min(fontSize/16,3)em (~48px) and wraps inside overflow-hidden p-[5%] with NO auto-shrink. A long operator-typed lockdown textBlob wrapped taller than the zone and was cut off top/bottom — the template-level TemplateSca
  _Fix:_ Set sizeMode:'absolute' (+fontWeight:800, lineHeight:1.15) on the fallback TEXT zone config so it routes through the signage-design ENGINE SignageText, whose FitScaler (measure + transform:scale, Taurus-safe) guarantees the message always fits its zone, never clipping.
- **[P0][FIXED] Cached lockdown message on the crash-recovery screen clips on non-1920x1080 canvases**  
  ↳ PlayerErrorBoundary.render() showed the cached emergency with FIXED sizes (text-7xl headline, w-32 icon, text-3xl body, p-12) centered with items-center/justify-center and NO fit. On a 960x1080 portrait / 320x1080 ribbon the stacked content wrapped taller than the viewport and was clipped top AND bottom — surfaces during a player crash WHILE an emergency is cached, the worst moment for the alert t
  _Fix:_ Added a local FitToViewport component (transform:scale; longhand top/right/bottom/left; no inset — Chromium-83/Taurus-safe; re-measures on resize+font-load) and wrapped the cached-emergency block in it; added break-words/whitespace-pre-wrap.
- **[P1][FIXED] Non-critical TEXT_BROADCAST banner can overflow a narrow ribbon / cover a short canvas**  
  ↳ The banner was a plain top-anchored flex items-center row with text-xl, NO height bound, NO word-break, and the text column had no min-w-0. A long broadcast (or one long unbreakable word/URL) overflowed horizontally on a 320x1080 ribbon and pushed the icon offscreen; with no height cap an unbounded banner could grow to cover the whole canvas on a short screen.
  _Fix:_ Capped the banner at maxHeight:40vh + overflow-hidden (can never become the whole canvas) and added min-w-0 + whitespace-pre-wrap + break-words to the text column so long content wraps instead of overflowing. Taurus-safe (no inset; ml-4 stands in for flex gap).
- **[P1][FIXED] All-clear 'type CLEAR' control can be pushed off-screen on a short viewport (operator can't clear)**  
  ↳ The overlay centers its content with items-center justify-center and NO scroll; on a short viewport (a phone in landscape, a small window) the stacked icon + text-5xl title + desc + the tall 'type CLEAR' card exceeded the viewport height and the input + Terminate button were clipped off-screen, leaving the operator unable to clear an active emergency.
  _Fix:_ Added overflow-y-auto to the overlay root and my-auto to the inner content block — it scrolls when content exceeds the viewport (control always reachable) and stays centered when it fits. Dashboard surface (no Taurus constraint).
- **[P1][FIXED] Verified end-to-end — no defect found (no change needed)**  
  ↳ WS onmessage, SSE handle(), and HTTP-poll all consume every type (OVERRIDE, ALL_CLEAR, SYNC, TENANT_CHANGED, SOS, TEXT_BROADCAST, MEDIA_ALERT, ALL_CLEAR_MESSAGE). ALL_CLEAR_MESSAGE -> setPushedEmergencyMessage(null); ALL_CLEAR -> fetchContent() -> manifest is sole arbiter -> setActiveEmergency(null). Lockdown/weather/evacuate set emergencyPlaylistId; the manifest swaps to that playlist's template 
  _Fix:_ No code change — confirmed correct by reading the controller + all three player transport handlers + TemplateScaler. Documented for the lead audit.

## LANE 2 — Make AI-generated signage templates magnificent & complex for every industry (engine-level depth + richer copy)
**SHIPPED** branch=worktree-wf_ad074da8-079-2 sha=f5ef67e6e4

- **[P1][FIXED] Non-image boards rendered as a flat near-black slab**  
  ↳ themeGradient() in art-director.ts was a single radial between surface (#1e293b) and background (#0f172a) — near-identical on dark themes → no depth, no brand tint. Confirmed by printing old vs new CSS and Playwright screenshot.
  _Fix:_ New surface-css.ts themeBackgroundCss(): accent-glow radial + spotlight/duotone/wash ramp per theme mood; wired into the mapper's bg slot + Template.bgGradient. 13 new engine tests pin it.
- **[P1][FIXED] three-up-grid cards were the banned 'flat rounded rectangle with a shadow'**  
  ↳ art-director.ts card config set bgColor: palette.surface with only a drop shadow — exactly the lowest-common-denominator look CLAUDE.md's design workflow bans.
  _Fix:_ cardFillCss(): top-lit surface gradient + accent hairline border + a thin accent top-bar; renderer paints cardBg/cardBorder/cardAccentBar.
- **[P2][FIXED] Kicker eyebrow floated unanchored; boards could be sparse (headline+button only)**  
  ↳ No decorative geometry anywhere in the engine; the prompt permitted but did not require kicker/body/cta, so glance boards regressed to a lone headline + CTA.
  _Fix:_ Added a tapered accent divider under every kicker (dividerCss + renderer rule); rewrote ART_DIRECTOR_SYSTEM_PROMPT COPY RULES to require a complete multi-element board with venue-specific copy.
- **[P2][NEEDS-FOLLOWUP] Wave-3 generated background photography still not wired (image archetypes ride a gradient placeholder)**  
  ↳ mapImageConfig still omits assetUrl for the background slot; the spec's image.mode:'generate' plan is parsed but image-gen injection is the documented Wave-3 follow-up (injectBackgroundImage exists but real provider call is gated).
  _Fix:_ Hero/lower-third/poster boards now ride the new accent-tinted layered gradient (far better than before) but real photography is the biggest remaining premium lift. Land the Wave-3 image-gen provider call so IMAGE archetypes get real photos behind the existing scrim.
- **[P2][NEEDS-FOLLOWUP] Only 12 curated themes; per-vertical theme variety could go deeper (e.g. duotone-photo, editorial, pattern textures)**  
  ↳ themes.ts has 12 moods; the new surfaceStyle adds 3 background recipes (spotlight/duotone/wash). Richer textures (subtle noise/grain, geometric pattern overlays, dual-accent gradients) would push further toward 10x but are design-subjective and risk contrast/Taurus regressions if rushed.
  _Fix:_ Propose: add a 'pattern' SurfaceStyle (low-alpha CSS-only geometric/dot overlay) + a dual-accent duotone, each behind a contrast-safe scrim, with new engine tests — land iteratively with screenshot review per the no-batch design workflow, not in one risky pass.

## LANE 3 — "Test every piece of our application" (read-only)

- **[P0][NEEDS-FOLLOWUP] MFA challenge step missing backward-compatibility guard for legacy single-factor accounts**  
  ↳ apps/web/src/app/login/page.tsx:116-157 handles MFA challenge when server returns mfaRequired:true, but apps/api/src/auth/auth.service.ts never persists MFA configuration state. If a device JWT auth or old session tries to re-authenticate, no MFA record exists. The token-mismatch error surfaces to end-user as 'Your sign-in attempt timed out' (line 147) which is technically correct but doesn't expl
  _Fix:_ Add MFA_ENABLED flag to User model + audit every attempt result in AuditLog with explicit 'mfa_attempted' / 'mfa_required' / 'mfa_passed' codes. Currently auditLoginAttempt() logs mfaRequired as a boolean in extra.details but never surfaces it in the UI error message, making operator debugging impossible.
- **[P1][NEEDS-FOLLOWUP] Unpaired screen registration cooldown allows orphaned rows, no cleanup visible to operator**  
  ↳ apps/api/src/screens/screens.controller.ts:117-205: Per-fingerprint cooldown is 5 seconds (intentional, to avoid bricking kiosks on retry storms). But a malicious or misconfigured device can create orphan Screen rows forever — each unknown FP gets a blank screen + pairing code never used. After 1 week of constant registration spam from an unclaimed kiosk, the Screens list grows by 10k stale rows. 
  _Fix:_ Add Screens page filter 'Show unclaimed (60+ days)' + a bulk-delete action for operators to reclaim list space. Audit each registration attempt with `claimed: false` in AuditLog. Consider a nightly cron that marks screens as STALE_UNCLAIMED after 90 days (operator visibility without data loss).
- **[P0][NEEDS-FOLLOWUP] Schedule group-supersession bug: group-level playlist doesn't knock out per-screen pins on ALL members**  
  ↳ apps/api/src/schedules/schedules.controller.ts:124-147: 2026-06-26 fix documented in comments: 'publishing to a GROUP also supersedes the per-screen pins on all of its member screens, so the single group schedule cleanly wins.' But replaceOr[] only de-activates existing schedules where (screenGroupId === targetId OR screenId === targetId). If Screen A was pinned with PlaylistX but belongs to Scree
  _Fix:_ Verify the replaceOr query actually runs and deactivates per-screen pins. Add a unit test that publishes to a group with N members, each having a pre-existing per-screen schedule, and asserts ALL per-screen rows are deactivated. The code comment suggests it was fixed but no test proves it.
- **[P1][NEEDS-FOLLOWUP] SVG upload blocked but not visible in UX; operator confusion about 'unsupported' format**  
  ↳ apps/api/src/assets/assets.controller.ts:39-82: SVG is explicitly commented out from ALLOWED_TYPES because AssetSanitizerService (asset-sanitizer.service.ts) exists but has zero callers. Line 82: '// Lane-1 P1: temporarily disabled'. In the web upload form, SVG isn't in the pre-check list, so an operator who drags an SVG logo sees 'File type is not supported' (generic message) with no hint that SV
  _Fix:_ Either: (A) wire AssetSanitizerService into the upload path NOW and re-enable SVG, or (B) surface a specific message in the web UI: 'SVG logos are coming soon — export as PNG for now.' Currently it's silent ambiguity.
- **[P1][NEEDS-FOLLOWUP] E2E schedule-publish test is completely skipped; no proof publish→manifest works end-to-end**  
  ↳ tests/e2e/schedule-publish.spec.ts: ALL three meaningful tests are .skip()-ed. Line 19-27 (schedule form visibility), 29-46 (POST schedule returns 201), 48-59 (manifest reflects schedule) are all marked TODO. The test file loads but runs nothing. Code comment: 'requires seeded playlist + screen + live schedule API'. Meanwhile, tests/e2e/emergency-path.spec.ts is fully written and verifies every em
  _Fix:_ Implement fixtures/seed pattern from emergency-path.spec.ts for schedule-publish.spec.ts. Create a canned tenant/screen/playlist, publish a schedule, poll manifest, assert items match. This is the #1 operator workflow. If it's broken, nobody knows.
- **[P1][NEEDS-FOLLOWUP] Stripe webhook idempotency is gated but no DLQ for failed reconciles; lost license mutations may go unaudited**  
  ↳ apps/api/src/billing/billing.controller.ts: billing-webhook.controller is unauthenticated. StripeService wraps the webhook in idempotency (event ID → dedup cache). But if Stripe → API succeeds, API → Prisma fails (e.g., DB timeout), the webhook returns 500 and Stripe retries. If the retry succeeds but the original mutation was NEVER persisted, the P0-7 audit trail (AuditLog every License mutation)
  _Fix:_ Add a BackgroundJobQueue (or use a cron) that queries failed Stripe webhook rows (eventId, status=FAILED) daily and surfaces them to SUPER_ADMIN with 'Reconcile Now' button. Log webhook handler errors to AuditLog as 'BILLING_WEBHOOK_FAILED' so forensics can see them. Current approach is silent.
- **[P0][NEEDS-FOLLOWUP] Manifest cache validation: asset fileHash may be null, causing SW precache misses and stale playback**  
  ↳ apps/api/src/screens/screens.controller.ts:2753: Emergency playlist items are mapped with `asset_hash: item.asset.fileHash ?? null`. If an asset was uploaded before the hash-backfill migration (maintenance/backfill-asset-hashes.ts) ran, or if it was imported from a legacy system, fileHash is NULL. The player's offline-cache.ts:precachePlaylist() receives a null sha256 and can't validate whether th
  _Fix:_ Add a post-upload hook that computes fileHash synchronously (or async + re-upload manifest on completion). In the manifest path, if fileHash is null, compute it on-the-fly (one-time cost), persist it, and return it. For emergency assets specifically, fileHash is NON-NEGOTIABLE because stale emergency content is a life-safety issue.
- **[P1][NEEDS-FOLLOWUP] Service Worker emergency-asset precache may time out; watchdog doesn't fail gracefully**  
  ↳ apps/web/src/app/player/offline-cache.ts:98-100: precacheEmergency() has a 60-second watchdog timeout. If the SW is downloading 50+ MB of videos over a slow connection and the timeout fires, resolve() is called with {ok:false}. The caller (player/page.tsx) then doesn't retry the precache and assumes it failed. On next app boot, the emergency cache is STALE. Comment at line 99: 'if the SW is still 
  _Fix:_ Implement a 5-minute periodic background-sync check (via updatefound event or a polling timer) that retries precacheEmergency() and logs results to AuditLog. Wire a 'Last Emergency Cache Sync' timestamp into the player's info overlay so operators can see whether precache is live or stale.
- **[P1][NEEDS-FOLLOWUP] Media picker toolbar floats over top nav on iPhone; Upload/Cancel buttons hidden**  
  ↳ Known issue #215 in task list: 'MOBILE BUG 1: media picker toolbar floats over top nav, hides Upload/Cancel'. This is operator-blocking on iPhone: they select assets but can't confirm because the buttons are under the page header. Not a regression; likely a z-index or viewport-fit issue. Affects every mobile workflow (playlists, templates, assets page).
  _Fix:_ Add `position: sticky; bottom: 0; z-index: 50; safe-area-inset-bottom: env(safe-area-inset-bottom);` to the picker toolbar. Test on iPhone 15 in landscape/portrait.
- **[P2][NEEDS-FOLLOWUP] Playlist wizard Step 2: selected media count shows below fold; operator never sees their edits until manual scroll**  
  ↳ Known issue #117 in task list: 'Wizard Step 2 — Selected media hidden below fold + missing checkboxes'. The UI lists selected items but also shows the full media library below them. On mobile, the 'You selected 5 items' summary is off-screen. Operator thinks they didn't select anything and re-selects the same items, creating duplicates.
  _Fix:_ Sticky-pin the 'X selected' summary to the top of Step 2 with a compact preview row showing thumbnails of the first 3 selected items. Let operator expand/collapse the full list without scrolling past the summary.
- **[P1][NEEDS-FOLLOWUP] Scraped SVG logos fall back to raster on adopt, breaking vector scalability**  
  ↳ Known issue #223 in task list: 'Branding: scraped SVG logo falls back to a raster on adopt (Dominos)'. When branding wizard scrapes a website's SVG logo (e.g., from Domino's Pizza), it downloads the SVG, but on adopt the image is converted to PNG/raster. The operator's branded templates then show a blurry/pixelated logo on large displays instead of crisp vector. The AssetSanitizerService being inc
  _Fix:_ Implement AssetSanitizerService to scrub SVG <script> tags and wrappers, then allow SVG upload. Store downloaded SVG as-is without raster conversion. In template preview, render SVG with viewBox scaling so logos are crisp at any size.
- **[P0][NEEDS-FOLLOWUP] Location/address picker doesn't find real US street addresses; fallback to lat/lng not obvious**  
  ↳ Known issue #216: 'MOBILE BUG 2: location/address picker not finding real US street addresses'. Operator is adding a new location (for fleet multi-location support) but the address autocomplete doesn't match 'Main St, Anywhere, TX'. Falls back to 'show me a map, drop a pin manually'. This is operator friction on every new location add.
  _Fix:_ Check the geocoding provider (likely OpenStreetMap or similar via an MCP). If it's rate-limited or doesn't cover US addresses, upgrade to a production provider (Google Places, Mapbox, HERE). Surface a 'Can't find your address? Zoom the map, click the location' hint so the fallback is discovered.
- **[P2][NEEDS-FOLLOWUP] Settings drawer pushes off left edge on lower list items; operator can't reach buttons**  
  ↳ Known issue #217: 'MOBILE BUG 3: screen settings drawer pushes off left edge, worse lower in list'. On iPhone, the settings side panel for a screen (e.g., emergency assets, orientation lock) is positioned off-screen if the screen is >5 rows down in the list. This is likely a modal positioning bug that assumes the triggering element is always near the top.
  _Fix:_ Use a BottomSheet component instead of a side drawer on mobile viewports. Or, use Headless UI Dialog with fixed positioning + scrolling inside the modal body so buttons are always reachable.
