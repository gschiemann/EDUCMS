# Gallery-poster freshness guard.
#
# The templates gallery shows a pre-rendered static poster PNG per board
# (apps/web/public/templates/_thumbs/<rel>.png), NOT a live iframe (perf: 114
# live 4K iframes pegged the main thread). Failure mode: edit a board's HTML but
# forget to regenerate its poster -> the gallery shows the OLD board forever.
# That burned a full session on 2026-07-24 (redesigned + photo-baked fashion
# boards, gallery still showed the June posters). Operator: "dont ever let the
# previews not get updated when we update templates."
#
# This gate FAILS if any board HTML changed without its poster PNG changing too.
# Dependency-free (git + node built-ins), <1 min.
name: Poster Freshness
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  # 2026-08-04 — every workflow now declares one. Without it, pushing three
  # commits in a minute leaves three full runs racing on the same ref.
  # cancel-in-progress is deliberately PULL-REQUEST ONLY: a superseded PR run
  # is noise, but every commit that lands on master must keep its own verdict
  # (a cancelled master run reads as "never checked" in the history).
  # Keyed on the REF for pull_request (so a superseded PR run is cancelled) and
  # on the individual SHA for push (so every commit that lands on master keeps
  # its own run to completion). 2026-08-04: the first attempt expressed this as
  # `cancel-in-progress: ${{ github.event_name == 'pull_request' }}`, and master
  # runs were still being cancelled — a cancelled run reads as "never checked"
  # in the history, which is the opposite of the intent. Making the GROUP unique
  # per push cannot be got wrong: two master commits are never in the same group,
  # so there is nothing to cancel.
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.ref || github.sha }}
  cancel-in-progress: true

jobs:
  poster-freshness:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # need the base ref for the diff
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check gallery posters were regenerated for changed boards
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE="origin/${{ github.base_ref }}"
          else
            BASE="${{ github.event.before }}"
          fi
          echo "poster-freshness base ref: $BASE"
          POSTER_CHECK_BASE="$BASE" node apps/web/tools/check-poster-freshness.cjs
